# ============================================
# AUTH CONFIGURATION (required for login)
# ============================================

# Auth.js secret - REQUIRED for JWT encryption
# Generate with: npx auth secret
AUTH_SECRET=

# Google OAuth - REQUIRED for "Sign in with Google"
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID (Web application)
# 3. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
# 4. Copy Client ID and Client Secret below
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Email/Password authentication uses a demo user for now
# Demo credentials: demo@example.com / demo123
# Full database-backed auth will be added in a later phase

# ============================================
# STRIPE CONFIGURATION (required for Pro subscriptions)
# ============================================

# Stripe API Keys - Get from: https://dashboard.stripe.com/apikeys
# Use test keys (sk_test_/pk_test_) for development
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Stripe Product/Price ID
# 1. Go to https://dashboard.stripe.com/products
# 2. Create a new product (e.g., "Pro Subscription")
# 3. Add a recurring price (e.g., $9.99/month)
# 4. Copy the Price ID (starts with price_)
STRIPE_PRO_PRICE_ID=price_...

# Stripe Webhook Secret
# Local: Run `stripe listen --forward-to localhost:3000/api/webhooks/stripe`
# Production: Get from Stripe Dashboard -> Developers -> Webhooks
STRIPE_WEBHOOK_SECRET=whsec_...

# App URL for Stripe redirect URLs
# Local: http://localhost:3000
# Production: Your deployed domain
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# DATABASE CONFIGURATION (auto-generated by Prisma)
# ============================================

# SQLite database for local development
# Prisma creates this automatically with `npx prisma db push`
DATABASE_URL="file:./prisma/dev.db"

# ============================================
# NOTION API (for feedback collection)
# ============================================

# Notion Internal Integration Token
# 1. Go to https://www.notion.so/my-integrations
# 2. Create a new integration
# 3. Copy the Internal Integration Token
# 4. Share the "Ad Library Analyzer - Feedback" database with the integration
NOTION_API_KEY=secret_...

# ============================================
# FACEBOOK API (already configured)
# ============================================
