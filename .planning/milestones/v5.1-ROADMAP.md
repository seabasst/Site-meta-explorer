# Milestone v5.1: Visual Consistency

**Status:** ✅ SHIPPED 2026-03-18
**Phases:** 44-45
**Total Plans:** 2

## Overview

Align V1 analyser page with V2 dashboard and landing page branding. Two phases: first settle layout and navigation (header, brand identity, CTAs), then update V1's color palette, typography, and spacing to match V2's design system.

## Phases

### Phase 44: V1 Navigation & Brand Identity

**Goal**: V1 analyser page has consistent branding with landing page and V2 dashboard
**Depends on**: Nothing (first phase in milestone)
**Plans**: 1 plan

Plans:
- [x] 44-01: Replace nav with branded header, clean up dead code, add upgrade card

**Details:**
- Replaced old V1 nav (5 dead links, mobile hamburger, /coming-soon CTA) with branded header
- Added BarChart3 + "Ad Library Pro" brand lockup with logo linking to /
- Added "Get Pro" CTA pill linking to /#pricing with cubic-bezier easing
- Added contextual upgrade card below analysis results with persuasive copy
- Used CSS custom properties for theme colors, hardcoded #1235e2 for brand accent
- 1 file modified (src/app/analyser/page.tsx), 2 commits

### Phase 45: V1 Theme Update

**Goal**: V1 visual design matches V2's color palette, typography, and spacing
**Depends on**: Phase 44
**Plans**: 1 plan

Plans:
- [x] 45-01: Update CSS variables and analyser page: green-to-blue palette, typography, spacing, transitions

**Details:**
- Added --accent-blue (#1235e2), --accent-blue-light (#3a5ce8), --accent-blue-dark (#0f2bc0) CSS vars
- Updated .btn-primary, .input-field, .spinner, .glow-gold utility classes from green to blue
- Replaced all 39 green/emerald accent references with #1235e2 blue family in analyser page
- Changed cards from rounded-2xl/rounded-xl to rounded-lg (amber warnings intentionally retain rounded-xl)
- Added transition-colors duration-200 to main element for smooth surface transitions
- Preserved --accent-green vars for 46 files outside milestone scope
- 2 files modified (globals.css, page.tsx), 2 commits

---

## Milestone Summary

**Key Decisions:**
- Used CSS custom properties for theme-dependent colors but hardcoded #1235e2 for brand accent (consistent with V2 pattern)
- Direct hex values in Tailwind classes instead of CSS var references (matches V2 approach)
- Preserved --accent-green CSS vars for backward compat with pages outside milestone scope
- Amber warning boxes intentionally retain rounded-xl to differentiate system alerts from content cards

**Issues Resolved:**
- Old V1 nav with dead links and /coming-soon CTA replaced with branded header
- Green accent palette inconsistency with V2 blue design system resolved
- Card radii inconsistency (rounded-2xl vs V2's rounded-lg) standardized

**Issues Deferred:**
- Dark mode toggle UI on V1 (V1 is free tool, not dashboard)
- Unified design tokens extraction (overkill for freemium entry point)
- V2 ternary cleanup (308 occurrences — separate milestone v5.2)

**Technical Debt Incurred:**
- Unused imports (SignInButton, UserMenu, SubscriptionStatus) in page.tsx — tree-shaken in prod
- --accent-blue CSS vars defined but page uses hardcoded hex (per decision 45-01-D1)
- --accent-green vars preserved for backward compat with 46 files outside scope

---

_For current project status, see .planning/ROADMAP.md_
