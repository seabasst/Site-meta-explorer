---
phase: 65-classification-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/ad-library/ads/route.ts
  - src/app/dashboard/v2/ad-library/types.ts
  - src/app/dashboard/v2/ad-library/components/ad-detail-lightbox.tsx
autonomous: true

must_haves:
  truths:
    - "User sees classification tags (all 8 categories) when viewing an ad in the detail lightbox"
    - "Tags show human-readable labels from TAXONOMY, not raw slugs"
    - "Ads without classification data show no tags section (graceful absence, not error)"
  artifacts:
    - path: "src/app/api/ad-library/ads/route.ts"
      provides: "Classification data included in ads API response"
      contains: "classification"
    - path: "src/app/dashboard/v2/ad-library/types.ts"
      provides: "Classification fields on Ad type"
      contains: "classification"
    - path: "src/app/dashboard/v2/ad-library/components/ad-detail-lightbox.tsx"
      provides: "Classification tags rendered in ad detail view"
      contains: "TAXONOMY"
  key_links:
    - from: "src/app/api/ad-library/ads/route.ts"
      to: "prisma.adClassification"
      via: "Prisma include on findMany"
      pattern: "classification.*select"
    - from: "src/app/dashboard/v2/ad-library/components/ad-detail-lightbox.tsx"
      to: "TAXONOMY"
      via: "import for label lookup"
      pattern: "import.*taxonomy"
---

<objective>
Display classification tags on individual ads in the ad detail lightbox.

Purpose: When a user clicks an ad to view details, they should see what classification categories were assigned (e.g., "UGC / User-Generated", "Talking Head", "Pain Point") so they can understand the creative strategy at a glance.

Output: Ad detail lightbox shows a "Classification" section with labeled tags for each of the 8 categories.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/classification/taxonomy.ts
@src/app/api/ad-library/ads/route.ts
@src/app/dashboard/v2/ad-library/types.ts
@src/app/dashboard/v2/ad-library/components/ad-detail-lightbox.tsx
@prisma/schema.prisma (AdClassification model — 1:1 relation on AdLibraryAd via `classification`)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Include classification data in ads API and extend Ad type</name>
  <files>
    src/app/api/ad-library/ads/route.ts
    src/app/dashboard/v2/ad-library/types.ts
  </files>
  <action>
  1. **Ads API** (`src/app/api/ad-library/ads/route.ts`):
     In the `prisma.adLibraryAd.findMany` call (around line 352), add `classification` to the `include` block alongside `brand` and `assets`:
     ```
     classification: {
       select: {
         assetType: true,
         visualFormat: true,
         hookTactic: true,
         messagingAngle: true,
         awarenessStage: true,
         creativeMechanic: true,
         offerType: true,
         intendedAudience: true,
       },
     },
     ```
     This is a 1:1 relation (AdClassification has `adId @unique`), so it returns `null` if no classification exists. No N+1 issue — Prisma handles this as a left join.

  2. **Update the response type** (`AdLibraryAdResponse` in route.ts):
     Add to the interface:
     ```
     classification: {
       assetType: string;
       visualFormat: string;
       hookTactic: string;
       messagingAngle: string;
       awarenessStage: string;
       creativeMechanic: string;
       offerType: string;
       intendedAudience: string;
     } | null;
     ```

  3. **Update the frontend Ad type** (`src/app/dashboard/v2/ad-library/types.ts`):
     Add to the `Ad` interface:
     ```
     classification: {
       assetType: string;
       visualFormat: string;
       hookTactic: string;
       messagingAngle: string;
       awarenessStage: string;
       creativeMechanic: string;
       offerType: string;
       intendedAudience: string;
     } | null;
     ```

  The `serializeAd` function already handles the full ad object via JSON.stringify, so the classification data will be serialized automatically — no changes needed there.
  </action>
  <verify>
  `curl "http://localhost:3000/api/ad-library/ads?limit=5" | jq '.ads[0].classification'` returns either a classification object with 8 fields or null. Verify `npm run build` passes with no type errors.
  </verify>
  <done>Ads API includes classification data (8 category fields or null) in every ad response, and the Ad frontend type matches.</done>
</task>

<task type="auto">
  <name>Task 2: Render classification tags in ad detail lightbox</name>
  <files>src/app/dashboard/v2/ad-library/components/ad-detail-lightbox.tsx</files>
  <action>
  Add a "Classification" section to the ad detail lightbox, in the RIGHT column details panel, positioned between the "Platforms" section and the "CTA & Link" section (or after CTA — wherever it fits best visually).

  1. **Guard**: Only render the section if `ad.classification` is not null. If null, render nothing (no empty state needed — unclassified ads just don't show the section).

  2. **Section layout**: Use the same pattern as other sections in the lightbox (border-t separator, icon + label header, content below):
     - Icon: Use `Tag` from lucide-react (already imported)
     - Label: "AI Classification" (using the existing `labelClass` pattern)
     - Layout: flex-wrap grid of pill tags

  3. **Tags**: For each of the 8 categories, render a small pill showing:
     - Category short label (e.g., "Format", "Hook", "Stage") — use a compact label map, NOT the full TAXONOMY description
     - Value label from TAXONOMY[key].labels[value] — human-readable
     - Style: small rounded pills similar to the platform badges already in the lightbox. Use subtle color coding per category — reuse the CATEGORY_PILLS colors from analysis-view.tsx:
       ```
       assetType: '#3b82f6', visualFormat: '#8b5cf6', hookTactic: '#f59e0b',
       messagingAngle: '#10b981', awarenessStage: '#ec4899', creativeMechanic: '#06b6d4',
       offerType: '#f97316', intendedAudience: '#a855f7'
       ```
     - Each pill: `backgroundColor: ${color}15` (very subtle), `color: ${color}`, text-xs font-semibold

  4. Import TAXONOMY and CATEGORY_KEYS from `@/lib/classification/taxonomy`.

  5. Define a compact label map inside the component (or at module level):
     ```
     const CATEGORY_SHORT_LABELS: Record<string, string> = {
       assetType: 'Asset', visualFormat: 'Format', hookTactic: 'Hook',
       messagingAngle: 'Messaging', awarenessStage: 'Stage',
       creativeMechanic: 'Mechanic', offerType: 'Offer', intendedAudience: 'Audience',
     };
     ```
  </action>
  <verify>
  Open an ad detail lightbox for a classified ad — verify 8 colored pills appear with human-readable labels. Open a lightbox for an unclassified ad — verify no classification section appears. Check dark mode and light mode both look correct.
  </verify>
  <done>Ad detail lightbox shows colored classification pills for classified ads, with human-readable labels from TAXONOMY, and gracefully hides the section for unclassified ads.</done>
</task>

</tasks>

<verification>
1. Ads API response includes `classification` field (object or null) on every ad
2. Ad detail lightbox shows 8 classification tags for classified ads
3. Tags use human-readable labels (e.g., "Talking Head" not "talking-head")
4. Tags use per-category color coding
5. Unclassified ads show no classification section (not an error)
6. Dark mode and light mode both render correctly
7. `npm run build` passes without type errors
</verification>

<success_criteria>
- Classification tags visible in ad detail lightbox for all 8 categories
- Human-readable labels from TAXONOMY
- Graceful absence for unclassified ads
- No performance regression (classification is a simple 1:1 include, not a new query)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/65-classification-ui/65-02-SUMMARY.md`
</output>
