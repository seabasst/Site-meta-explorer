---
phase: 58-ugc-creator-briefs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/creative-lab-types.ts
  - src/app/api/creative-lab/generate-brief/route.ts
autonomous: true

must_haves:
  truths:
    - "API accepts a pageId and returns a structured UGC brief as JSON"
    - "Brief includes hooks, shot list, talking points, B-roll suggestions, and CTA"
    - "API returns 404 with helpful message when brand has no analysis cache"
    - "Brief content is brand-specific (uses real ad copy, guidelines, category data)"
  artifacts:
    - path: "src/lib/creative-lab-types.ts"
      provides: "UGCBriefScene and UGCBrief type definitions"
      contains: "UGCBrief"
    - path: "src/app/api/creative-lab/generate-brief/route.ts"
      provides: "POST endpoint for Claude-powered brief generation"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/creative-lab/generate-brief/route.ts"
      to: "prisma.adLibraryBrand"
      via: "database query"
      pattern: "prisma\\.adLibraryBrand\\.findUnique"
    - from: "src/app/api/creative-lab/generate-brief/route.ts"
      to: "prisma.brandAnalysisCache"
      via: "database query"
      pattern: "prisma\\.brandAnalysisCache\\.findUnique"
    - from: "src/app/api/creative-lab/generate-brief/route.ts"
      to: "prisma.adLibraryAd"
      via: "top ads query for real copy examples"
      pattern: "prisma\\.adLibraryAd\\.findMany"
    - from: "src/app/api/creative-lab/generate-brief/route.ts"
      to: "@anthropic-ai/sdk"
      via: "Claude API call"
      pattern: "client\\.messages\\.create"
---

<objective>
Create the UGC brief generation API and shared types.

Purpose: Backend that takes a brand's ad library data (analysis cache, top-performing ads, brand guidelines) and uses Claude to generate a structured UGC creator brief with hooks, shot list, talking points, and B-roll suggestions.

Output: New API route at `/api/creative-lab/generate-brief` and UGC brief types in `creative-lab-types.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-ugc-creator-briefs/58-RESEARCH.md
@src/app/api/creative-lab/generate-config/route.ts
@src/lib/creative-lab-types.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UGC brief types to creative-lab-types.ts</name>
  <files>src/lib/creative-lab-types.ts</files>
  <action>
    Append two new interfaces to the existing `creative-lab-types.ts` file (do NOT modify existing types):

    1. `UGCBriefScene` interface with fields:
       - `sceneNumber: number`
       - `duration: string` (e.g. "2-3s")
       - `shotType: string` (e.g. "Close-up", "Wide shot", "POV")
       - `description: string`
       - `visualNotes: string`
       - `audioNotes: string`

    2. `UGCBrief` interface with fields:
       - Metadata: `brandName`, `category`, `briefTitle`, `contentType`, `platform`, `duration`, `aspectRatio` (all strings)
       - `hooks: string[]` (3 hook options)
       - `scenes: UGCBriefScene[]` (5-8 scenes)
       - `talkingPoints: string[]` (3-5 key messages)
       - `brollSuggestions: string[]` (4-6 B-roll shots)
       - `callToAction: string`
       - `tone: string`
       - `dosAndDonts: { dos: string[]; donts: string[] }`
       - `keyProductInfo: string`
       - `targetAudience: string`

    Follow the exact structure from the research document (58-RESEARCH.md Code Examples section).
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors introduced.</verify>
  <done>UGCBriefScene and UGCBrief interfaces exported from creative-lab-types.ts, TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create generate-brief API route</name>
  <files>src/app/api/creative-lab/generate-brief/route.ts</files>
  <action>
    Create `src/app/api/creative-lab/generate-brief/route.ts` following the EXACT pattern of `generate-config/route.ts`. Key differences:

    **Request validation:** Same as generate-config -- `z.object({ pageId: z.string().min(1) })`.

    **Data fetching (3 queries):**
    1. Brand lookup: `prisma.adLibraryBrand.findUnique({ where: { pageId }, select: { id, pageName, category } })` -- return 404 if not found.
    2. Analysis cache: `prisma.brandAnalysisCache.findUnique({ where: { brandId: brand.id } })` -- return 404 with message "Run analysis first. No cached analysis found for this brand." if not found.
    3. **NEW: Top-performing ads** (not in generate-config): `prisma.adLibraryAd.findMany({ where: { brandId: brand.id, isActive: true, body: { not: null } }, orderBy: { reachEstimate: 'desc' }, take: 10, select: { body, title, ctaText, displayFormat } })`.
    4. Brand guidelines: Same optional auth-gated fetch as generate-config (try/catch, non-blocking).

    **Claude prompt:** Construct a detailed prompt that includes:
    - Brand name, category, total ads analyzed
    - Diversity scores (format, tone, journeyPhase, visualStyle, messenger, overall)
    - Distribution data (from cache.distributionJson)
    - Andromeda metrics (refresh rate, stale %, hook quality, unique concepts, funnel balance)
    - Brand guidelines (voice, colors, audience) if available
    - Top 10 ad bodies/titles/CTAs as real copy examples
    - Instructions to generate a UGC brief matching the UGCBrief interface exactly
    - Specify section lengths: hooks (3 options, 1-2 sentences each), scenes (5-8, each with all 6 fields), talking points (3-5, 1 sentence each), B-roll (4-6 suggestions), do's (3-5), don'ts (3-5)
    - Tell Claude to tailor B-roll suggestions to the brand's category (reference the B-Roll Categories table from research)
    - Request ONLY valid JSON, no markdown/explanation

    **Response parsing:** Same pattern as generate-config:
    - Extract text from response blocks
    - Strip ```json fences
    - JSON.parse with try-catch
    - Type-assert as UGCBrief
    - Return as JSON response

    **Settings:** `export const dynamic = 'force-dynamic'` and `export const maxDuration = 60`.

    Use `claude-sonnet-4-20250514` model with `max_tokens: 4000`.
  </action>
  <verify>
    1. `npx tsc --noEmit` passes.
    2. `curl -X POST http://localhost:3000/api/creative-lab/generate-brief -H "Content-Type: application/json" -d '{"pageId":"INVALID"}' ` returns 404.
    3. Test with a real pageId from the database that has an analysis cache -- verify response contains all UGCBrief fields (hooks array, scenes array, talkingPoints array, brollSuggestions array, callToAction, etc.).
  </verify>
  <done>POST /api/creative-lab/generate-brief returns a complete UGCBrief JSON for brands with analysis cache, returns 404 for missing brands/cache, and handles errors gracefully.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles with no errors: `npx tsc --noEmit`
2. API route responds correctly to invalid input (400/404)
3. API route generates a full UGC brief for a brand with cached analysis data
4. Brief JSON matches the UGCBrief type structure (all required fields present)
</verification>

<success_criteria>
- UGCBriefScene and UGCBrief types are exported from creative-lab-types.ts
- POST /api/creative-lab/generate-brief accepts {pageId} and returns structured UGCBrief JSON
- Brief includes brand-specific content derived from real ad data, analysis scores, and guidelines
- Error handling matches generate-config pattern (404 for missing brand/cache, 500 for unexpected errors)
</success_criteria>

<output>
After completion, create `.planning/phases/58-ugc-creator-briefs/58-01-SUMMARY.md`
</output>
