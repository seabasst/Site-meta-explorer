---
phase: 51-demographic-peek
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/ad-library/brands/[pageId]/route.ts
  - src/app/dashboard/v2/ad-library/components/demographic-peek.tsx
autonomous: true

must_haves:
  truths:
    - "Brand API returns demographicsJson when present"
    - "DemographicPeek component renders age, gender, and region mini charts"
    - "Charts handle null/missing demographics gracefully (component returns null)"
    - "Charts adapt to dark and light mode"
  artifacts:
    - path: "src/app/dashboard/v2/ad-library/components/demographic-peek.tsx"
      provides: "Collapsible demographic peek panel with 3 mini charts"
      min_lines: 80
    - path: "src/app/api/ad-library/brands/[pageId]/route.ts"
      provides: "demographicsJson in brand API response"
      contains: "demographicsJson"
  key_links:
    - from: "demographic-peek.tsx"
      to: "demographics-normalizer.ts"
      via: "NormalizedDemographics type import"
      pattern: "import.*NormalizedDemographics.*demographics-normalizer"
    - from: "brands/[pageId]/route.ts"
      to: "prisma.adLibraryBrand"
      via: "demographicsJson field in serialized response"
      pattern: "demographicsJson"
---

<objective>
Add demographicsJson to the brand API response and create the DemographicPeek component with mini Recharts bar/pie charts for age, gender, and region breakdowns.

Purpose: Enables demographic data to flow from the API to a visual panel in the ad library browse view.
Output: Updated brand API endpoint + self-contained DemographicPeek component ready to wire into page.tsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-demographic-peek/51-RESEARCH.md
@src/app/api/ad-library/brands/[pageId]/route.ts
@src/lib/demographics-normalizer.ts
@src/app/dashboard/v2/ad-library/types.ts
@src/components/dashboard/demographic-trend-chart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add demographicsJson to brand API serialization</name>
  <files>src/app/api/ad-library/brands/[pageId]/route.ts</files>
  <action>
    In the brand detail endpoint GET handler, the `serializeBrand` function currently omits `demographicsJson`. Update it:

    1. Add `demographicsJson: unknown` to the `SerializedBrand` interface (after `category`).
    2. In the `serializeBrand` function input type, add `demographicsJson: unknown` (it's `Json?` in Prisma, comes through as `unknown`).
    3. In the `serializeBrand` function body, pass through `demographicsJson: brand.demographicsJson ?? null`.

    The Prisma query (`prisma.adLibraryBrand.findUnique`) already returns all fields including `demographicsJson`, so no query changes needed.

    Do NOT add demographics to the PATCH or DELETE handlers.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors. Then test with curl: `curl "http://localhost:3000/api/ad-library/brands/[any-pageId]" | jq '.brand.demographicsJson'` should return the demographics JSON (or null if brand has none).</verify>
  <done>Brand detail API response includes `demographicsJson` field for all brand queries.</done>
</task>

<task type="auto">
  <name>Task 2: Create DemographicPeek component with mini charts</name>
  <files>src/app/dashboard/v2/ad-library/components/demographic-peek.tsx</files>
  <action>
    Create a new component `DemographicPeek` that renders a collapsible horizontal panel with 3 mini Recharts charts side by side.

    **Props interface:**
    ```typescript
    interface DemographicPeekProps {
      demographics: NormalizedDemographics;
      darkMode: boolean;
      collapsed: boolean;
      onToggleCollapse: () => void;
    }
    ```

    Import `NormalizedDemographics` from `@/lib/demographics-normalizer`.

    **Layout:**
    - Outer wrapper: a `V2Card`-style container (rounded-xl, border, bg matching dark/light mode) with a header row.
    - Header row: "Audience Demographics" label (left) + chevron toggle button (right) using `ChevronDown`/`ChevronUp` from lucide-react.
    - When NOT collapsed: 3 columns in a responsive grid (`grid grid-cols-1 md:grid-cols-3 gap-4`), each containing a mini chart.
    - When collapsed: header row only, charts hidden.

    **Mini charts (all 120px tall via ResponsiveContainer):**

    1. **Age Breakdown** - Vertical `BarChart` using `demographics.ageBreakdown`.
       - XAxis: `dataKey="age"`, fontSize 10, no tickLine/axisLine.
       - Bar: `dataKey="percentage"`, fill `#1235e2`, radius `[4,4,0,0]`.
       - Tooltip showing `${value}%`.
       - Title label: "Age" in small muted text above chart.

    2. **Gender Breakdown** - Horizontal `BarChart` (layout="vertical") using `demographics.genderBreakdown`.
       - YAxis: `dataKey="gender"`, fontSize 10, type="category", width=50.
       - XAxis: type="number", hide=true.
       - Bar: `dataKey="percentage"`, each bar colored by gender using existing colors: male=#3b82f6, female=#ec4899, unknown=#6b7280. Use a cell-level fill approach (map genderBreakdown items and apply fill per cell).
       - Tooltip showing `${value}%`.
       - Title label: "Gender" above chart.

    3. **Region Breakdown** - Vertical `BarChart` using top 5 items from `demographics.regionBreakdown` (slice first 5).
       - XAxis: `dataKey="region"`, fontSize 10, no tickLine/axisLine.
       - Bar: `dataKey="percentage"`, fill `#22c55e`, radius `[4,4,0,0]`.
       - Tooltip showing `${value}%`.
       - Title label: "Top Regions" above chart.

    **If any breakdown array is empty, skip rendering that chart column.** If ALL are empty, return null (don't render the panel at all).

    **Tooltip styling** (consistent with project):
    ```typescript
    contentStyle={{
      backgroundColor: darkMode ? '#161b2e' : '#fff',
      border: `1px solid ${darkMode ? 'rgba(18,53,226,0.2)' : '#e2e8f0'}`,
      borderRadius: '8px',
      fontSize: '12px',
      color: darkMode ? '#e2e8f0' : '#1e293b',
    }}
    ```

    **Dark/light mode colors:**
    - Container bg: `darkMode ? 'bg-[#101322]/50 border-[#1235e2]/10' : 'bg-white border-slate-200'`
    - Title text: `darkMode ? 'text-slate-400' : 'text-slate-500'`
    - Header text: `darkMode ? 'text-slate-300' : 'text-slate-700'`
    - XAxis/YAxis tick fill: `darkMode ? '#94a3b8' : '#64748b'`

    Mark as `'use client'` at top. Export `DemographicPeek` as named export.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors. Visually inspect by temporarily importing into page.tsx with mock data (will be properly wired in Plan 02).</verify>
  <done>DemographicPeek component exists, accepts NormalizedDemographics, renders 3 mini charts with proper dark/light mode support, handles empty data gracefully.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Brand API endpoint at `/api/ad-library/brands/[pageId]` returns `demographicsJson` in response
- `demographic-peek.tsx` exports `DemographicPeek` component
- Component imports from `@/lib/demographics-normalizer` and `recharts`
</verification>

<success_criteria>
- Brand API returns demographicsJson for any brand query
- DemographicPeek component renders age/gender/region mini charts
- Component handles null/empty breakdowns gracefully
- Dark and light mode styling is correct
- No type errors in the project
</success_criteria>

<output>
After completion, create `.planning/phases/51-demographic-peek/51-01-SUMMARY.md`
</output>
