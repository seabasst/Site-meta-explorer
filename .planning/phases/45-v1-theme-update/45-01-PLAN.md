---
phase: 45-v1-theme-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/analyser/page.tsx
autonomous: true

must_haves:
  truths:
    - "V1 page uses #1235e2 blue family everywhere green accent previously appeared"
    - "V1 heading and body font sizes follow V2 typographic scale (text-sm labels, text-base body, consistent heading weights)"
    - "V1 cards use rounded-lg, buttons/pills use rounded-full, spacing follows gap-3/gap-4/gap-6 rhythm"
    - "Page background applies transition-colors duration-200 for smooth surface transitions"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Updated CSS variables with blue color family replacing green"
      contains: "#1235e2"
    - path: "src/app/analyser/page.tsx"
      provides: "V1 analyser page with blue accent palette, aligned typography and spacing"
      min_lines: 1500
  key_links:
    - from: "src/app/globals.css --accent-green vars"
      to: "src/app/analyser/page.tsx var() references"
      via: "CSS custom properties"
      pattern: "--accent-blue.*#1235e2"
    - from: "src/app/analyser/page.tsx"
      to: "V2 design system"
      via: "Shared #1235e2 blue, rounded-lg cards, rounded-full pills"
      pattern: "#1235e2|rounded-lg|rounded-full"
---

<objective>
Update V1 analyser page's visual design to match V2's color palette, typography, and spacing. Replace the green accent palette with #1235e2 blue family, align typographic scale, standardize border radii and spacing rhythm.

Purpose: Make V1 feel like part of the same product as V2 dashboard and landing page.
Output: Updated `globals.css` (CSS variables) and `src/app/analyser/page.tsx` (component classes).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
@src/app/analyser/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS variables and utility classes from green to blue</name>
  <files>src/app/globals.css</files>
  <action>
Update the CSS custom properties and utility classes in globals.css to replace the green accent palette with the #1235e2 blue family. This is the foundation — page.tsx references these variables extensively.

**CSS variable changes in `:root`:**
- `--accent-green: #1a3933` -> `--accent-blue: #1235e2` (keep old name as alias for backward compat with other pages, but add new name)
- `--accent-green-light: #2d5a4f` -> `--accent-blue-light: #0f2bc0` (darker blue for hover states)
- Actually, to minimize blast radius on OTHER pages that use these vars, do this instead:
  1. Add NEW variables: `--accent-blue: #1235e2` and `--accent-blue-light: #0f2bc0`
  2. KEEP the old `--accent-green` and `--accent-green-light` variables as-is (other pages like coming-soon, auth, dashboard v1 still reference them)
  3. The page.tsx task will swap references directly

**Utility class updates (these are only used on V1):**
- `.btn-primary`: Change `background: var(--accent-green)` to `background: #1235e2`
- `.btn-primary:hover`: Change `background: var(--accent-green-light)` to `background: #0f2bc0`, update box-shadow to `rgba(18, 53, 226, 0.3)`
- `.input-field:focus`: Change `border-color: var(--accent-green)` to `border-color: #1235e2`, update box-shadow to `rgba(18, 53, 226, 0.1)`
- `.spinner`: Change `border-top-color: var(--accent-green)` to `border-top-color: #1235e2`
- `.glow-gold`: Update shadow from `rgba(26, 57, 51, 0.15)` to `rgba(18, 53, 226, 0.12)` for a subtle blue glow instead of green
- `.glow-gold-sm`: Update shadow from `rgba(26, 57, 51, 0.1)` to `rgba(18, 53, 226, 0.08)`

Do NOT change:
- The `.dark` variant variables (out of scope per requirements)
- Category colors (`--cat-*`)
- Chart colors (`--chart-*`)
- oklch-based shadcn variables
- Any `--bg-*`, `--text-*`, `--border-*` variables (these stay as-is)
  </action>
  <verify>
Grep globals.css for `#1235e2` -- at least 4 matches (accent-blue var, btn-primary, input-field, spinner).
Grep globals.css for `--accent-green` -- old vars still exist (backward compat).
Grep globals.css for `--accent-blue:` -- new variable exists.
Run `npx next build 2>&1 | tail -5` -- no CSS parse errors (build may have pre-existing TS error, that's OK).
  </verify>
  <done>
CSS foundation updated: new --accent-blue variables added, utility classes (.btn-primary, .input-field:focus, .spinner, .glow-gold) use blue instead of green. Old --accent-green vars preserved for other pages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update analyser page.tsx — color, typography, spacing, transitions</name>
  <files>src/app/analyser/page.tsx</files>
  <action>
This task covers all four requirements (THEME-01 through THEME-04) in the analyser page component. Work through each systematically.

**THEME-01: Green to Blue color swap**

Replace every green/emerald reference with the #1235e2 blue family:

1. `LoadingSpinner` component (line ~52): Change `border-t-[var(--accent-green-light)]` to `border-t-[#1235e2]`

2. `ActiveChartFilter` component (line ~58): Change `border-emerald-500/50` to `border-[#1235e2]/50` and `text-emerald-400` to `text-[#1235e2]`

3. Hero heading (line ~463): Change `text-[var(--accent-green-light)]` on the italic "Analyser" span to `text-[#1235e2]`

4. Input mode toggle links (lines ~532, ~604): Change `hover:text-[var(--accent-green-light)]` to `hover:text-[#1235e2]`

5. Toggle button groups — Active/All, EU/Global (lines ~621, ~633, ~652, ~664): Change `bg-[var(--accent-green)]` to `bg-[#1235e2]` for the active state

6. EU Only tooltip (line ~674): Change `text-[var(--accent-green-light)]` to `text-[#1235e2]`

7. Date inputs focus (lines ~699, ~708): Change `focus:border-[var(--accent-green)]` to `focus:border-[#1235e2]`

8. Example brand buttons (line ~732): Change `hover:text-[var(--accent-green)]` and `hover:border-[var(--accent-green)]` to `hover:text-[#1235e2]` and `hover:border-[#1235e2]`

9. Brand Comparison heading (line ~777): Change `text-[var(--accent-green-light)]` to `text-[#1235e2]`

10. Comparison brand button active state (line ~788): Change `bg-[var(--accent-green)]` to `bg-[#1235e2]`

11. Comparison brand badge "vs" (line ~812): Change `bg-emerald-500/20 text-emerald-400 border border-emerald-500/50` to `bg-[#1235e2]/20 text-[#1235e2] border border-[#1235e2]/50`

12. Page size buttons (line ~896): Change `hover:text-[var(--accent-green)]` and `hover:border-[var(--accent-green)]` to `hover:text-[#1235e2]` and `hover:border-[#1235e2]`

13. Export button (line ~944): Change `bg-emerald-500/20 border-emerald-500/50 text-emerald-400 hover:bg-emerald-500/30` to `bg-[#1235e2]/20 border-[#1235e2]/50 text-[#1235e2] hover:bg-[#1235e2]/30`

14. Ad format filter active state (line ~1045): Change `bg-emerald-500/20 border-emerald-500/50 text-emerald-400` to `bg-[#1235e2]/20 border-[#1235e2]/50 text-[#1235e2]`

15. Tab navigation active states (lines ~1106, ~1119, ~1132): Change `bg-[var(--accent-green)]` to `bg-[#1235e2]`

16. ALL section heading italic spans — "Demographics", "Performers", "Analysis", "Longevity", "Pages", "Analysis" (lines ~1166, 1311, 1332, 1352, 1369, 1386, 1405): Change `text-[var(--accent-green-light)]` to `text-[#1235e2]`

17. Chart summary top segment highlights (lines ~1204, ~1231): Change `text-[var(--accent-green-light)]` to `text-[#1235e2]`

18. All Ads details summary (line ~1419): Change `hover:text-[var(--accent-green-light)]` to `hover:text-[#1235e2]`

19. Filtered ads count badge (line ~1425): Change `text-emerald-400` to `text-[#1235e2]`

20. Ad link buttons (line ~1487): Change `text-[var(--accent-green-light)]` to `text-[#1235e2]`

**THEME-02: Typography alignment**

The V2 typographic scale uses: `text-sm` for labels, `text-base` for body, `font-semibold` for headings (not serif italic).

Do NOT change the serif italic pattern for section headings — this is a distinctive V1 style that works well. Instead, ensure consistency:
- Section headings (`h3`): Keep `font-serif text-lg` — this is the V1 identity
- Sub-headings (`h4`): Ensure `text-sm font-medium` (already correct in most places)
- Labels: Ensure `text-xs` for meta labels (already mostly correct)
- Body text: The `text-lg` on the hero subtitle is correct; results body text should be `text-sm` or `text-base`

No major typography changes needed — the existing scale is already close to V2. The key alignment is making sure labels are consistently `text-xs` or `text-sm`.

**THEME-03: Border radii and spacing**

1. The form card (line ~472): Change `rounded-2xl` to `rounded-lg` to match V2's card rounding
2. Toggle button groups (lines ~614, ~645): Already `rounded-lg` on container, inner buttons `rounded-md` — this is fine, keep as-is for segmented controls
3. Section `<details>` elements: Already `rounded-xl` — change to `rounded-lg` for consistency with V2 cards
4. Glass cards (`glass rounded-xl`): Change to `glass rounded-lg`
5. Tooltip (line ~672): Change `rounded-xl` to `rounded-lg`
6. Upgrade card (line ~1530): Change `rounded-xl` to `rounded-lg`
7. Buttons that act as pills (example brand buttons line ~732, export options): Already `rounded-full` — correct
8. Verify spacing uses `gap-3`, `gap-4`, or `gap-6` rhythm. The existing spacing is largely gap-2, gap-3, gap-4 which is close enough. Only adjust where gap-5 or other non-standard gaps appear.

**THEME-04: Smooth surface transitions**

Add `transition-colors duration-200` to the `<main>` element (line ~434):
- Change: `<main className="min-h-screen">` to `<main className="min-h-screen transition-colors duration-200">`

This ensures background color changes (e.g., if dark mode is later added) transition smoothly rather than flashing.

**Important: Do NOT touch:**
- The header/nav section (lines 435-453) — already done in Phase 44
- The upgrade card content/copy (lines 1528-1546) — already done in Phase 44
- Any V2 dashboard files
- The BarChart3 icon or brand lockup
  </action>
  <verify>
Run these checks:

1. `grep -c 'accent-green' src/app/analyser/page.tsx` — should be 0 (all green refs replaced)
2. `grep -c 'emerald' src/app/analyser/page.tsx` — should be 0 (all emerald refs replaced)
3. `grep -c '#1235e2' src/app/analyser/page.tsx` — should be 20+ (blue used throughout)
4. `grep -c 'rounded-xl' src/app/analyser/page.tsx` — should be 0 or very few (most changed to rounded-lg)
5. `grep 'transition-colors duration-200' src/app/analyser/page.tsx` — at least 1 match on main element
6. `npx next build 2>&1 | tail -5` — no new build errors (pre-existing TS error about useSearchParams is OK)
7. `grep -c 'accent-green' src/app/globals.css` — old vars still exist (not deleted, other pages need them)
  </verify>
  <done>
V1 analyser page fully updated: all green/emerald replaced with #1235e2 blue, cards use rounded-lg, pills use rounded-full, main element has transition-colors duration-200. Zero accent-green or emerald references remain in page.tsx. Requirements THEME-01 through THEME-04 all satisfied.
  </done>
</task>

</tasks>

<verification>
1. Zero `accent-green` or `emerald` references in `src/app/analyser/page.tsx`
2. `#1235e2` blue used for all accent colors (active states, hover states, italic heading spans, badges)
3. CSS variables `--accent-blue` and `--accent-blue-light` exist in globals.css
4. Old `--accent-green` vars preserved in globals.css for other pages
5. Utility classes (`.btn-primary`, `.input-field:focus`, `.spinner`) use blue
6. Cards use `rounded-lg`, pills/buttons use `rounded-full`
7. `<main>` has `transition-colors duration-200`
8. Build passes (pre-existing useSearchParams error is acceptable)
9. Header and upgrade card from Phase 44 are untouched
</verification>

<success_criteria>
- THEME-01: V1 uses #1235e2 blue color family — zero green accent references in page.tsx
- THEME-02: Typography scale consistent (text-sm labels, text-base/text-lg body, serif headings maintained)
- THEME-03: Cards rounded-lg, pills rounded-full, spacing follows gap-3/4/6 rhythm
- THEME-04: Main element has transition-colors duration-200
- Build compiles without new errors
- All four requirements (THEME-01 through THEME-04) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/45-v1-theme-update/45-01-SUMMARY.md`
</output>
