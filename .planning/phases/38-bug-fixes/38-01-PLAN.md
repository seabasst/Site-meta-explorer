---
phase: 38-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/v2/saved/page.tsx
  - src/app/api/categories/route.ts
  - src/app/api/categories/[slug]/route.ts
  - src/app/dashboard/v2/categories/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Unauthenticated user visiting Saved Ads sees 'Sign in to view your saved ads' prompt instead of empty state"
    - "Authenticated user visiting Saved Ads sees their saved ads as before"
    - "Category detail pages load correctly regardless of category name casing in the database"
    - "Category slugs in listing URLs are normalized to lowercase"
  artifacts:
    - path: "src/app/dashboard/v2/saved/page.tsx"
      provides: "Auth-aware saved ads page"
      contains: "useSession"
    - path: "src/app/api/categories/route.ts"
      provides: "Normalized category slugs"
      contains: "toLowerCase"
  key_links:
    - from: "src/app/dashboard/v2/saved/page.tsx"
      to: "next-auth/react"
      via: "useSession hook"
      pattern: "useSession"
    - from: "src/app/api/categories/route.ts"
      to: "src/app/api/categories/[slug]/route.ts"
      via: "slug normalization matching"
      pattern: "toLowerCase"
---

<objective>
Fix Saved Ads auth fallback (FIX-01) and Category detail 404s (FIX-03).

Purpose: Two quick fixes that make existing features work correctly -- Saved Ads shows a sign-in prompt instead of a misleading empty state, and category detail pages stop 404ing due to slug case mismatches.
Output: Modified saved page with auth awareness, normalized category slugs in both listing and detail APIs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-bug-fixes/38-RESEARCH.md

@src/app/dashboard/v2/saved/page.tsx
@src/app/dashboard/v2/ad-library/page.tsx (reference for useSession + login modal pattern)
@src/app/api/categories/route.ts
@src/app/api/categories/[slug]/route.ts
@src/app/dashboard/v2/categories/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth awareness to Saved Ads page (FIX-01)</name>
  <files>src/app/dashboard/v2/saved/page.tsx</files>
  <action>
  Add auth-awareness to the SavedAdsPage component:

  1. Import `useSession` from `next-auth/react` and `LogIn` from `lucide-react`
  2. Add `const { data: session, status } = useSession();` at the top of the component
  3. Before the existing loading/content rendering, add an early return when `status !== 'loading' && !session?.user` that renders an auth prompt inside V2Shell:
     - Use V2Card with centered content
     - Show a LogIn icon (size 48, muted color)
     - Heading: "Sign in to view your saved ads"
     - Subtext: "Save ads from the Ad Library to build your inspiration collection"
     - A sign-in button styled with the primary blue (#1235e2) that calls `signIn()` from next-auth/react
  4. Also import `signIn` from `next-auth/react`
  5. While `status === 'loading'`, show the existing skeleton/loading state (don't flash the auth prompt)
  6. The existing fetch logic stays unchanged -- it already handles the 401 gracefully, but now users won't reach that path unauthenticated

  Follow the exact pattern used in `v2/ad-library/page.tsx` for session handling and the login modal styling. Use V2Shell wrapping and darkMode-aware classes from useV2().
  </action>
  <verify>
  - `npm run build` passes with no TypeScript errors
  - Open `/dashboard/v2/saved` in incognito (unauthenticated) -- should see sign-in prompt, NOT "No saved ads yet"
  - Sign in with demo@example.com / demo123 -- should see saved ads list (or genuine empty state if none saved)
  </verify>
  <done>Unauthenticated users see a clear sign-in prompt on the Saved Ads page; authenticated users see their saved ads as before</done>
</task>

<task type="auto">
  <name>Task 2: Normalize category slugs to fix 404s (FIX-03)</name>
  <files>src/app/api/categories/route.ts, src/app/api/categories/[slug]/route.ts, src/app/dashboard/v2/categories/[slug]/page.tsx</files>
  <action>
  Fix category slug normalization so listing URLs match detail lookups:

  1. In `src/app/api/categories/route.ts`:
     - On line 57 where `slug: category` is set, change to `slug: category.toLowerCase().replace(/\s+/g, '_')`
     - This ensures the slug in listing URLs is always lowercase with underscores (e.g., "Fashion" becomes "fashion", "Car Rental" becomes "car_rental")
     - The `formatCategoryLabel` function already does the reverse (underscores to spaces + capitalize), so display labels remain correct

  2. In `src/app/api/categories/[slug]/route.ts`:
     - After extracting slug from params on line 11, add: `const normalizedSlug = slug.toLowerCase().replace(/_/g, ' ');`
     - Update the Prisma query to search with the original category name format: `where: { category: { equals: normalizedSlug, mode: 'insensitive' } }`
     - BUT ALSO keep a fallback: first try with the normalized slug, and if no results, try with the raw slug (for backward compat). Actually simpler: since `mode: 'insensitive'` handles casing, just ensure the underscore-to-space conversion happens. The DB stores "Fashion" or "Car Rental" (with spaces), and the URL has "fashion" or "car_rental" (with underscores). So convert underscores back to spaces before querying.
     - Change line 15 query to: `where: { category: { equals: slug.replace(/_/g, ' '), mode: 'insensitive' } }`

  3. In `src/app/dashboard/v2/categories/[slug]/page.tsx`:
     - Review the error state. If it shows a generic error, improve it to show "Category not found" with a link back to `/dashboard/v2/categories`
     - No major changes expected here -- just verify it handles the 404 response gracefully

  Important: The `formatCategoryLabel` function in the listing API already converts underscores to spaces and capitalizes, so display names will remain correct even with the normalized slugs.
  </action>
  <verify>
  - `npm run build` passes with no TypeScript errors
  - Check the categories listing API response: all slugs should be lowercase with underscores
  - Navigate to a category detail page from the listing -- should load correctly (no 404)
  - Test with a category that has mixed case in the database (e.g., "Fashion") -- URL should be `/categories/fashion` and page should load
  </verify>
  <done>Category listing API returns normalized lowercase slugs; category detail API correctly resolves underscored slugs back to database values; no more 404s when clicking category links</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Saved Ads page shows sign-in prompt when unauthenticated
- Category detail pages load from category listing links
- No regressions in existing category or saved ads functionality
</verification>

<success_criteria>
- FIX-01: Unauthenticated users see auth prompt on Saved Ads page, authenticated users see their ads
- FIX-03: All category detail pages reachable from the category listing without 404 errors
</success_criteria>

<output>
After completion, create `.planning/phases/38-bug-fixes/38-01-SUMMARY.md`
</output>
