---
phase: 53-infra-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/v2/ad-library/components/stats-bar.tsx
  - src/app/dashboard/v2/ad-library/components/pagination.tsx
  - src/app/dashboard/v2/ad-library/types.ts
autonomous: true

must_haves:
  truths:
    - "stats-bar.tsx and pagination.tsx no longer exist in the codebase"
    - "AdLibraryStats interface is removed from types.ts"
    - "No imports reference the deleted files or removed interface"
    - "Build succeeds with no errors"
  artifacts:
    - path: "src/app/dashboard/v2/ad-library/types.ts"
      provides: "Shared types without dead AdLibraryStats interface"
      contains: "export interface FilteredStats"
  key_links:
    - from: "src/app/dashboard/v2/ad-library/page.tsx"
      to: "src/app/dashboard/v2/ad-library/types.ts"
      via: "import { Ad, FilteredStats, TopBrand, PaginationData, FilterOption, DaysRange, SortField, GridDensity }"
      pattern: "import.*FilteredStats.*from.*types"
---

<objective>
Remove orphaned files and dead code left over from v6.0 UX overhaul.

Purpose: CODE-01 cleanup -- stats-bar.tsx and pagination.tsx are no longer imported anywhere, and the AdLibraryStats interface in types.ts is only consumed by the dead stats-bar.tsx. Removing them eliminates confusion and dead weight.

Output: Three fewer files/interfaces, clean build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/dashboard/v2/ad-library/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned component files</name>
  <files>
    src/app/dashboard/v2/ad-library/components/stats-bar.tsx
    src/app/dashboard/v2/ad-library/components/pagination.tsx
  </files>
  <action>
    Delete these two files:
    - `src/app/dashboard/v2/ad-library/components/stats-bar.tsx` (StatsBar component -- replaced by inline analytics bar in Phase 49)
    - `src/app/dashboard/v2/ad-library/components/pagination.tsx` (AdPagination component -- replaced by load-more pattern in Phase 48)

    Neither file is imported anywhere in the codebase. Confirm with grep before deleting:
    ```
    grep -r "stats-bar" src/
    grep -r "StatsBar" src/
    grep -r "AdPagination" src/
    grep -r "from.*pagination" src/app/dashboard/v2/ad-library/
    ```
    The only matches should be the files themselves. If any live imports exist, do NOT delete -- investigate first.
  </action>
  <verify>
    - `ls src/app/dashboard/v2/ad-library/components/stats-bar.tsx` returns "No such file"
    - `ls src/app/dashboard/v2/ad-library/components/pagination.tsx` returns "No such file"
    - `grep -r "stats-bar\|StatsBar\|AdPagination" src/` returns no results
  </verify>
  <done>Both orphaned component files are deleted with zero remaining references</done>
</task>

<task type="auto">
  <name>Task 2: Remove AdLibraryStats interface from types.ts and verify build</name>
  <files>src/app/dashboard/v2/ad-library/types.ts</files>
  <action>
    Edit `src/app/dashboard/v2/ad-library/types.ts`:

    1. Remove the `AdLibraryStats` interface (lines 5-12). This interface is only used by the now-deleted stats-bar.tsx. The active page.tsx uses `FilteredStats` instead.

    2. KEEP everything else: TopBrand, Ad, PaginationData, FilterOption, DaysRange, SortField, SORT_OPTIONS, GridDensity, FilteredStats, formatFormatLabel. These are all actively imported by page.tsx and filter-bar.tsx.

    3. Note: The `AdLibraryStats` interface references `TopBrand` via its `topBrandsByAdCount` field -- but TopBrand is independently used by page.tsx and filter-bar.tsx, so it stays.

    After editing types.ts, run `npx next build` to confirm no build errors.
  </action>
  <verify>
    - `grep "AdLibraryStats" src/app/dashboard/v2/ad-library/types.ts` returns no results
    - `grep -r "AdLibraryStats" src/` returns no results
    - `npx next build` succeeds
  </verify>
  <done>AdLibraryStats interface removed, all remaining types intact, build passes cleanly</done>
</task>

</tasks>

<verification>
- Both orphaned files deleted (stats-bar.tsx, pagination.tsx)
- AdLibraryStats interface removed from types.ts
- No dead imports or broken references: `grep -r "AdLibraryStats\|StatsBar\|AdPagination\|stats-bar\|from.*pagination" src/app/dashboard/v2/ad-library/` returns nothing
- `npx next build` succeeds
</verification>

<success_criteria>
CODE-01 complete: all three orphaned artifacts removed, build clean, no dead references.
</success_criteria>

<output>
After completion, create `.planning/phases/53-infra-cleanup/53-01-SUMMARY.md`
</output>
