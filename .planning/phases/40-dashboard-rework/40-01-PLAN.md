---
phase: 40-dashboard-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/kpi-card.tsx
  - src/components/dashboard/format-distribution-chart.tsx
  - src/components/dashboard/ads-timeline-chart.tsx
  - src/components/dashboard/platform-breakdown-chart.tsx
  - src/components/dashboard/top-brands-table.tsx
  - src/app/dashboard/v2/page.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard shows KPI metric cards (total ads, active ads, total brands, total reach) at top"
    - "Dashboard shows format distribution as a Recharts pie/donut chart"
    - "Dashboard shows ads over time as a Recharts area chart"
    - "Dashboard shows platform breakdown as a Recharts horizontal bar chart"
    - "Dashboard shows top brands table with ad count, active count, reach"
    - "No individual ad cards appear on the dashboard"
  artifacts:
    - path: "src/components/dashboard/kpi-card.tsx"
      provides: "Reusable KPI metric card component"
      exports: ["KpiCard"]
    - path: "src/components/dashboard/format-distribution-chart.tsx"
      provides: "Pie/donut chart for ad format breakdown"
      exports: ["FormatDistributionChart"]
    - path: "src/components/dashboard/ads-timeline-chart.tsx"
      provides: "Area chart for ads over time"
      exports: ["AdsTimelineChart"]
    - path: "src/components/dashboard/platform-breakdown-chart.tsx"
      provides: "Horizontal bar chart for platform distribution"
      exports: ["PlatformBreakdownChart"]
    - path: "src/components/dashboard/top-brands-table.tsx"
      provides: "Sortable brands table"
      exports: ["TopBrandsTable"]
    - path: "src/app/dashboard/v2/page.tsx"
      provides: "Reworked analytics dashboard page"
  key_links:
    - from: "src/app/dashboard/v2/page.tsx"
      to: "/api/ad-library/stats"
      via: "fetch with fast=true initial, then full load"
      pattern: "fetch.*api/ad-library/stats"
    - from: "src/app/dashboard/v2/page.tsx"
      to: "src/components/dashboard/*.tsx"
      via: "imports all chart components"
      pattern: "import.*from.*components/dashboard"
---

<objective>
Replace the current dashboard page (ad cards + benchmarking) with an analytics overview using KPI cards and Recharts charts.

Purpose: The dashboard currently duplicates the Ad Library by showing top ad cards. This plan transforms it into an analytics view showing aggregated metrics, charts, and tables over the full ad database. Addresses DASH-01.

Output: 5 new/updated dashboard widget components + fully reworked page.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-dashboard-rework/40-RESEARCH.md

Key source files:
@src/app/dashboard/v2/page.tsx
@src/app/dashboard/v2/v2-shell.tsx
@src/app/api/ad-library/stats/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analytics widget components</name>
  <files>
    src/components/dashboard/kpi-card.tsx
    src/components/dashboard/format-distribution-chart.tsx
    src/components/dashboard/ads-timeline-chart.tsx
    src/components/dashboard/platform-breakdown-chart.tsx
    src/components/dashboard/top-brands-table.tsx
  </files>
  <action>
Create 5 dashboard widget components. All must be 'use client' components that use `useV2()` for dark mode support.

1. **kpi-card.tsx** - Export `KpiCard` component:
   - Props: `{ label: string; value: string | number; icon: LucideIcon; trend?: { value: number; label: string } }`
   - Use `V2Card` wrapper with `p-6`
   - Label as `text-xs uppercase font-bold tracking-wide` (slate-400 dark, slate-500 light)
   - Value as `text-3xl font-black`
   - Icon colored `text-[#1235e2]`
   - Optional trend line with green (positive) / red (negative) color
   - Use `formatNumber` from v2-shell for value display

2. **format-distribution-chart.tsx** - Export `FormatDistributionChart`:
   - Props: `{ data: { format: string; count: number }[] }`
   - Recharts `PieChart` with `Pie` using `ResponsiveContainer width="100%" height={300}`
   - Colors: `['#1235e2', '#10b981', '#f59e0b', '#ef4444', '#8b5cf6']`
   - Include Tooltip and Legend, both dark-mode aware
   - Wrap in `V2Card` with title "Format Distribution"
   - Format labels capitalized (use existing `formatFormatLabel` logic inline)

3. **ads-timeline-chart.tsx** - Export `AdsTimelineChart`:
   - Props: `{ data: { date: string; count: number; activeCount: number }[] }`
   - Recharts `AreaChart` with two `Area` series: total (stroke #1235e2, fill #1235e2 opacity 0.1) and active (stroke #10b981, fill #10b981 opacity 0.1)
   - `CartesianGrid` strokeDasharray="3 3", stroke dark: #1e293b, light: #e2e8f0
   - XAxis/YAxis strokes: dark #64748b, light #94a3b8, fontSize 12
   - Dark-mode aware Tooltip (bg #1e293b/#fff, border #334155/#e2e8f0, rounded 8px)
   - Wrap in `V2Card` with title "Ad Activity Over Time"
   - Format date labels as short month-day (e.g., "Mar 15")

4. **platform-breakdown-chart.tsx** - Export `PlatformBreakdownChart`:
   - Props: `{ data: { platform: string; count: number }[] }`
   - Recharts horizontal `BarChart` (layout="vertical") with `Bar` fill #1235e2
   - `ResponsiveContainer` height={Math.max(200, data.length * 40)} for dynamic height
   - Capitalize platform names, replace underscores with spaces
   - Dark-mode aware axes and tooltip (same pattern as timeline)
   - Wrap in `V2Card` with title "Platform Breakdown"

5. **top-brands-table.tsx** - Export `TopBrandsTable`:
   - Props: `{ data: TopBrand[] }` where TopBrand = `{ id: string; pageId: string; pageName: string; category: string | null; adCount: number; activeAdCount: number; totalReach: string }`
   - Render as HTML table (same pattern as current dashboard's Top Brands table)
   - Columns: Brand (linked to `/dashboard/v2/ad-library?brand={pageId}`), Category, Total Ads, Active, Total Reach
   - Sortable by clicking column headers (local state, default sort by adCount desc)
   - Use `formatNumber` for numeric values
   - Dark mode: hover bg-[#1235e2]/10, divide colors #1235e2/10

All components import from 'recharts' (AreaChart, Area, BarChart, Bar, PieChart, Pie, Cell, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Legend as needed). Import V2Card and formatNumber from '../app/dashboard/v2/v2-shell' (check the actual export path). Import useV2 from '../app/dashboard/v2/v2-context'.

IMPORTANT: Check the actual import paths for V2Card, formatNumber, and useV2 before writing. They may need relative paths from src/components/dashboard/.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm all components type-check. Verify each file exports the named component.
  </verify>
  <done>5 widget components exist, type-check passes, each exports its named component with proper dark mode support and Recharts integration.</done>
</task>

<task type="auto">
  <name>Task 2: Rework dashboard page with analytics widgets</name>
  <files>
    src/app/dashboard/v2/page.tsx
  </files>
  <action>
Completely replace the content of `src/app/dashboard/v2/page.tsx` with an analytics dashboard. Remove ALL ad card rendering and benchmarking sections.

The new page structure:

1. **Data fetching** - Two-phase load:
   - Initial: `fetch('/api/ad-library/stats?fast=true')` for quick KPI display
   - Deferred: `fetch('/api/ad-library/stats')` (full mode) for charts that need platform/reach/timeline data
   - Use `Promise.all` pattern, show V2Skeleton initially, then progressively reveal widgets as data arrives

2. **Layout**:
   - Wrap in `<V2Shell title="Analytics Dashboard">` (change title from "Analysis Dashboard")
   - Remove the `insights` prop from V2Shell (no more ownBrand/competitors data)
   - KPI cards row: 4-column grid on lg, 2 on md, 1 on mobile — Total Ads (Database icon), Active Ads (TrendingUp icon), Total Brands (Users icon), Total Reach (Globe icon, from full mode)
   - Below KPIs: 2-column grid on lg — AdsTimelineChart (left, col-span 1) + FormatDistributionChart (right, col-span 1)
   - Below that: 2-column grid on lg — PlatformBreakdownChart (left) + placeholder div for future filters (right, shows "Filters coming soon" text in a V2Card)
   - Bottom: TopBrandsTable (full width)

3. **Progressive loading**:
   - KPI cards for totalAds, activeAds, totalBrands show immediately (from fast mode)
   - totalReach KPI shows "..." until full stats load, then updates
   - Charts show V2Skeleton until full stats arrive
   - TopBrandsTable shows V2Skeleton until full stats arrive

4. **Remove entirely**:
   - The `Ad` interface and `AdCard` component (the entire function at the bottom)
   - The `TrackedBrandSnapshot`, `TrackedBrand`, `DashboardData` interfaces
   - The `dashboardData` state and all benchmarking logic
   - The sorting tabs and ad grid sections
   - The ads fetch (`/api/ad-library/ads`)
   - The `formatFormatLabel` function (move inline to chart component if needed)

5. **Keep**: The `AdLibraryStats` and `TopBrand` interfaces (needed for stats API response).

Import all 5 new widget components from '@/components/dashboard/...'.
  </action>
  <verify>
Run `npm run build` (or `npx next build` if that's the build command). Page should compile. Visit `http://localhost:3000/dashboard/v2` — should show KPI cards, charts, and table with real data. No ad cards visible.
  </verify>
  <done>Dashboard page shows analytics widgets (KPI cards, 3 charts, brands table). No ad cards or benchmarking visible. Progressive loading works (fast stats load first, charts populate after).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds
- Dashboard at `/dashboard/v2` shows:
  - 4 KPI metric cards in a row
  - Area chart showing ads over time
  - Pie chart showing format distribution
  - Bar chart showing platform breakdown
  - Top brands table
- No individual ad cards appear
- Dark mode toggle works correctly on all widgets
- Charts are responsive (resize browser window)
</verification>

<success_criteria>
Dashboard displays aggregated analytics (KPI cards, charts, table) over the full ad database instead of individual ad cards. DASH-01 is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/40-dashboard-rework/40-01-SUMMARY.md`
</output>
