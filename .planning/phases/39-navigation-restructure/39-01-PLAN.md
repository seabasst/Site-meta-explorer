---
phase: 39-navigation-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/v2/v2-shell.tsx
autonomous: false

must_haves:
  truths:
    - "Sidebar shows 'Inspiration' section header with Ad Library, Saved Ads, Brands, Categories as indented sub-items"
    - "Creative Lab appears prominently just below Dashboard"
    - "Hikaru AI appears prominently in the sidebar"
    - "Competitors, Benchmarking, Share of Voice are not visible in sidebar"
    - "Downloads appears in sidebar but is grayed out with 'not available yet' indicator"
    - "Settings remains at bottom of sidebar"
  artifacts:
    - path: "src/app/dashboard/v2/v2-shell.tsx"
      provides: "Restructured sidebar navigation with sections"
      contains: "Inspiration"
  key_links:
    - from: "v2-shell.tsx sidebar nav"
      to: "/dashboard/v2/ad-library"
      via: "Inspiration section sub-item"
      pattern: "ad-library.*Inspiration|Inspiration.*ad-library"
---

<objective>
Restructure the v2 sidebar navigation from a flat list into a sectioned layout with the Inspiration group, hidden unused features, and a grayed-out Downloads item.

Purpose: Align navigation with the product's "Inspiration" concept and hide features that are not ready or being removed.
Output: Updated v2-shell.tsx with restructured sidebar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/dashboard/v2/v2-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure sidebar navigation data and rendering</name>
  <files>src/app/dashboard/v2/v2-shell.tsx</files>
  <action>
Replace the flat `NAV_ITEMS` array and its rendering with a sectioned navigation structure. The new sidebar layout from top to bottom should be:

**Top-level items (no section header):**
1. Dashboard (`/dashboard/v2`, LayoutDashboard icon) - same as today
2. Creative Lab (`/dashboard/v2/creative-lab`, Wand2 icon) - hero feature, prominent placement

**"Inspiration" section (with a small section header label):**
3. Ad Library (`/dashboard/v2/ad-library`, BookOpen icon) - keep "Ad Library" as the sub-item label, the section header says "Inspiration"
4. Saved Ads (`/dashboard/v2/saved`, Heart icon)
5. Brands (`/dashboard/v2/brands`, Globe icon)
6. Categories (`/dashboard/v2/categories`, Layers icon)

**Standalone items:**
7. Hikaru AI (`/dashboard/v2/hikaru`, MessageSquare icon) - prominent, keep as-is

**Disabled items:**
8. Downloads (`/dashboard/v2/downloads`, Download icon) - render as a span/div (NOT a Link), with `opacity-40 cursor-not-allowed` classes. Add a small "(soon)" text next to the label in a muted color. Use a `title` attribute with "Not available yet" for hover tooltip.

**Bottom (separated with mt-auto or spacer):**
9. Settings (`/dashboard/v2/settings`, Settings icon)

**Hidden items (keep in a comment block for future use):**
- Share of Voice, Benchmarking, Competitors - wrap in a block comment `{/* HIDDEN: ... */}` preserving the original data

**Implementation approach:**
- Define the nav structure as an array of sections/items, NOT the current flat array. Use a type like:
  ```typescript
  type NavItem = {
    id: string;
    icon: LucideIcon;
    label: string;
    disabled?: boolean;
    disabledLabel?: string;
  };
  type NavSection = {
    label?: string;  // section header, optional
    items: NavItem[];
  };
  ```
- The `<nav>` rendering should iterate over sections, rendering an optional section header (small uppercase text, muted color) then the items. Section header should use `text-xs font-semibold uppercase tracking-wider` with muted color (`text-slate-400` dark, `text-slate-500` light`).
- Inspiration sub-items should be indented slightly more than top-level items (add `pl-2` to the section's item container or similar).
- For disabled items, render a `<div>` instead of `<Link>`, apply `opacity-40 cursor-not-allowed`, and show "(soon)" after the label.
- Settings should be pushed to the bottom using flexbox: wrap the nav content in a div with `flex-1 overflow-y-auto` and put Settings in a separate `shrink-0` div at the bottom of the aside, with a top border separator.
- Add `ChevronDown` or `ChevronRight` to the lucide-react import (it is NOT currently imported in this file, even though it is used elsewhere in the project). However, you do NOT need to use it for this task - the Inspiration section should always be expanded (no collapse toggle needed). Only import it if you decide a chevron indicator adds visual clarity next to the section header.
- Keep all existing exports (V2Shell, V2Card, V2SectionTitle, V2Skeleton, formatNumber) unchanged.
- Keep the SidebarInsights interface and related code unchanged.
- Keep the header, main content area, dark mode toggle all unchanged.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no TypeScript errors. Run `npm run build` to verify the build succeeds. Visually inspect that the nav structure in code matches the specified layout.
  </verify>
  <done>
v2-shell.tsx compiles without errors. Sidebar renders: Dashboard, Creative Lab (top-level), then "Inspiration" section with Ad Library/Saved Ads/Brands/Categories, then Hikaru AI, then Downloads (grayed out with "(soon)"), then Settings at bottom. Competitors/Benchmarking/Share of Voice are commented out but preserved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Restructured sidebar navigation with Inspiration section, hidden features, grayed Downloads, and Settings at bottom</what-built>
  <how-to-verify>
1. Visit http://localhost:3000/dashboard/v2 (or your running dev server)
2. Check sidebar layout from top to bottom:
   - Dashboard and Creative Lab at top (no section header)
   - "INSPIRATION" section header in small uppercase text
   - Ad Library, Saved Ads, Brands, Categories indented under Inspiration
   - Hikaru AI standalone
   - Downloads grayed out with "(soon)" text - hover should show "Not available yet" tooltip, clicking should do nothing
   - Settings at the very bottom, separated from the rest
3. Verify Competitors, Benchmarking, Share of Voice are NOT visible
4. Click each visible nav item to verify navigation still works
5. Toggle dark mode - verify all nav items look correct in both modes
6. Check that active state highlighting still works on all clickable items
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- Sidebar renders the correct structure in both dark and light modes
- All nav links work except Downloads (disabled)
- Hidden items are preserved in comments
</verification>

<success_criteria>
- Sidebar shows Inspiration section with Ad Library, Saved Ads, Brands, Categories as sub-items
- Creative Lab is prominently placed below Dashboard
- Competitors, Benchmarking, Share of Voice are hidden from sidebar (code preserved in comments)
- Downloads is visible but grayed out with "(soon)" indicator
- Settings is at the bottom of the sidebar
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/39-navigation-restructure/39-01-SUMMARY.md`
</output>
