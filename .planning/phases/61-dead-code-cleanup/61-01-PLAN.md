---
phase: 61-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/v2/creative-lab/format-selector.tsx
  - src/app/dashboard/v2/creative-lab/generation-results.tsx
  - src/app/dashboard/v2/creative-lab/page.tsx
autonomous: true

must_haves:
  truths:
    - "format-selector.tsx no longer exists in the codebase"
    - "generation-results.tsx no longer exists in the codebase"
    - "When API returns 'Run analysis first. No cached analysis found for this brand.' the user sees the friendly message instead of the raw API string"
    - "Build passes with zero errors"
  artifacts:
    - path: "src/app/dashboard/v2/creative-lab/format-selector.tsx"
      provides: "DELETED - must not exist"
    - path: "src/app/dashboard/v2/creative-lab/generation-results.tsx"
      provides: "DELETED - must not exist"
    - path: "src/app/dashboard/v2/creative-lab/page.tsx"
      provides: "Fixed error string comparison at line 140"
      contains: "No cached analysis"
  key_links:
    - from: "src/app/dashboard/v2/creative-lab/page.tsx"
      to: "src/app/api/creative-lab/generate-config/route.ts"
      via: "error string comparison"
      pattern: "No cached analysis"
---

<objective>
Delete two orphaned components from Phase 56 and fix a stale error string comparison in the Creative Lab page.

Purpose: Close 3 tech debt items from the v7.0 audit — final cleanup phase.
Output: Cleaner codebase with no dead code and correct error handling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/app/dashboard/v2/creative-lab/page.tsx
@src/app/api/creative-lab/generate-config/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned components</name>
  <files>
    src/app/dashboard/v2/creative-lab/format-selector.tsx
    src/app/dashboard/v2/creative-lab/generation-results.tsx
  </files>
  <action>
    Delete these two files:
    - `src/app/dashboard/v2/creative-lab/format-selector.tsx` (superseded by config-screen.tsx in Phase 56)
    - `src/app/dashboard/v2/creative-lab/generation-results.tsx` (superseded by generation-gallery.tsx in Phase 56)

    Before deleting, do a quick grep to confirm neither file is imported anywhere:
    - `grep -r "format-selector" src/`
    - `grep -r "generation-results" src/`

    If either IS imported somewhere (unexpected), fix the import before deleting.
  </action>
  <verify>
    - `ls src/app/dashboard/v2/creative-lab/format-selector.tsx` returns "No such file"
    - `ls src/app/dashboard/v2/creative-lab/generation-results.tsx` returns "No such file"
    - `grep -r "format-selector" src/` returns no results
    - `grep -r "generation-results" src/` returns no results
  </verify>
  <done>Both orphaned files deleted with no remaining references in the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale error string comparison in page.tsx</name>
  <files>src/app/dashboard/v2/creative-lab/page.tsx</files>
  <action>
    In `src/app/dashboard/v2/creative-lab/page.tsx` around line 140, change the error comparison from:

    ```
    data.error === 'No cached analysis found'
    ```

    to use `.includes()` for resilience:

    ```
    data.error?.includes('No cached analysis')
    ```

    This matches the actual API response string from `generate-config/route.ts` line 57:
    `'Run analysis first. No cached analysis found for this brand.'`

    The friendly fallback message on line 141 should remain as-is:
    `'This brand hasn\'t been analyzed yet. Run a diversity analysis first from the Ad Library.'`
  </action>
  <verify>
    - `grep "includes.*No cached analysis" src/app/dashboard/v2/creative-lab/page.tsx` returns a match
    - `grep "=== 'No cached analysis found'" src/app/dashboard/v2/creative-lab/page.tsx` returns NO match (old string gone)
    - `npx next build` completes with no errors
  </verify>
  <done>Error string comparison matches actual API response. Build passes cleanly.</done>
</task>

</tasks>

<verification>
1. `ls src/app/dashboard/v2/creative-lab/` — format-selector.tsx and generation-results.tsx are NOT listed
2. `grep -r "format-selector\|generation-results" src/` — no results
3. `grep "includes.*No cached analysis" src/app/dashboard/v2/creative-lab/page.tsx` — matches
4. `npx next build` — exits 0 with no errors
</verification>

<success_criteria>
- format-selector.tsx deleted
- generation-results.tsx deleted
- Error string comparison uses `.includes('No cached analysis')` instead of strict equality
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/61-dead-code-cleanup/61-01-SUMMARY.md`
</output>
