---
phase: 46-component-extraction
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - src/app/dashboard/v2/ad-library/page.tsx
  - src/app/dashboard/v2/ad-library/[pageId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Ad library page renders identically before and after extraction"
    - "All existing filters, search, pagination, and save-ad functionality still work"
    - "No regressions in dark mode or responsive layout"
    - "Brand detail page reuses shared AdCard component"
  artifacts:
    - path: "src/app/dashboard/v2/ad-library/page.tsx"
      provides: "Slim orchestrator page composing extracted components"
      min_lines: 200
    - path: "src/app/dashboard/v2/ad-library/[pageId]/page.tsx"
      provides: "Brand detail page using shared AdCard"
      contains: "import.*AdCard.*from"
  key_links:
    - from: "src/app/dashboard/v2/ad-library/page.tsx"
      to: "src/app/dashboard/v2/ad-library/components/ad-card.tsx"
      via: "import { AdCard } from './components/ad-card'"
      pattern: "import.*AdCard.*from.*components/ad-card"
    - from: "src/app/dashboard/v2/ad-library/page.tsx"
      to: "src/app/dashboard/v2/ad-library/components/stats-bar.tsx"
      via: "import { StatsBar } from './components/stats-bar'"
      pattern: "import.*StatsBar.*from.*components/stats-bar"
    - from: "src/app/dashboard/v2/ad-library/page.tsx"
      to: "src/app/dashboard/v2/ad-library/components/pagination.tsx"
      via: "import { AdPagination } from './components/pagination'"
      pattern: "import.*AdPagination.*from.*components/pagination"
    - from: "src/app/dashboard/v2/ad-library/page.tsx"
      to: "src/app/dashboard/v2/ad-library/components/filter-dropdown.tsx"
      via: "import { FilterDropdown } from './components/filter-dropdown'"
      pattern: "import.*FilterDropdown.*from.*components/filter-dropdown"
    - from: "src/app/dashboard/v2/ad-library/page.tsx"
      to: "src/app/dashboard/v2/ad-library/components/filter-chip.tsx"
      via: "import { FilterChip } from './components/filter-chip'"
      pattern: "import.*FilterChip.*from.*components/filter-chip"
    - from: "src/app/dashboard/v2/ad-library/page.tsx"
      to: "src/app/dashboard/v2/ad-library/types.ts"
      via: "import types from './types'"
      pattern: "import.*from.*./types"
    - from: "src/app/dashboard/v2/ad-library/[pageId]/page.tsx"
      to: "src/app/dashboard/v2/ad-library/components/ad-card.tsx"
      via: "import { AdCard } from '../components/ad-card'"
      pattern: "import.*AdCard.*from.*components/ad-card"
---

<objective>
Rewire the ad library page to import all extracted components, reducing it from ~1044 lines to a slim orchestrator. Also update the brand detail page to reuse the shared AdCard component.

Purpose: Complete the component extraction by wiring everything together. The page becomes a state management orchestrator that composes extracted components.
Output: Rewritten page.tsx (~350-450 lines, down from 1044) + updated [pageId]/page.tsx using shared AdCard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-component-extraction/46-01-SUMMARY.md
@src/app/dashboard/v2/ad-library/page.tsx
@src/app/dashboard/v2/ad-library/[pageId]/page.tsx
@src/app/dashboard/v2/ad-library/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire page.tsx to use extracted components</name>
  <files>src/app/dashboard/v2/ad-library/page.tsx</files>
  <action>
    Rewrite `page.tsx` to import and compose the extracted components. The page keeps ALL state management and data fetching logic — only the JSX rendering delegates to components.

    **Imports to add:**
    ```
    import { Ad, AdLibraryStats, TopBrand, PaginationData, FilterOption, DaysRange } from './types';
    import { AdCard } from './components/ad-card';
    import { FilterDropdown } from './components/filter-dropdown';
    import { FilterChip } from './components/filter-chip';
    import { StatsBar } from './components/stats-bar';
    import { AdPagination } from './components/pagination';
    ```

    **Imports to REMOVE** (now handled by component files):
    - Remove icon imports that are only used in extracted components: `Play, Image as ImageIcon, Layers, ExternalLink, ChevronDown, ChevronLeft, ChevronRight, Users, BarChart3, Activity, Archive`
    - Keep icons still used directly in page.tsx: `BookOpen, Search, Filter, X, Calendar, Tag, Heart, LogIn, Layers` (Layers is used for hide-carousel toggle), `Users` (used for brand filter dropdown icon)
    - Actually, audit each icon carefully. After extraction, the page.tsx itself uses: `BookOpen` (error state + section title + empty state), `Search` (search input), `Tag` (industry filter icon), `Layers` (format filter icon + hide carousel toggle), `Calendar` (days filter icon), `Users` (brand filter icon), `Heart` (login modal), `LogIn` (login modal), `X` (login modal close), `Filter` (NOT used — remove)
    - Remove the inline `AdCard` function definition (lines 811-958)
    - Remove the inline `FilterDropdown` function definition (lines 965-1015)
    - Remove the inline `FilterChip` function definition (lines 1021-1044)
    - Remove the inline `generatePageNumbers` function (lines 789-805)
    - Remove the inline `formatFormatLabel` function (lines 106-109)
    - Remove the inline type definitions (lines 34-100) — import from types.ts instead

    **JSX changes:**
    - Replace the stats grid section (lines 380-392) with: `<StatsBar stats={stats} darkMode={darkMode} />`
    - Replace the pagination section (lines 650-698) with: `<AdPagination page={page} totalPages={totalPages} onPageChange={setPage} darkMode={darkMode} />`
    - The filter bar JSX stays inline in page.tsx (it's tightly coupled to the many filter state setters) BUT uses imported FilterDropdown and FilterChip components
    - The ad grid JSX stays inline (it's just a map over ads using imported AdCard)
    - The login modal JSX stays inline (it's tightly coupled to auth state)

    **State management stays in page.tsx:**
    - All useState hooks remain
    - All useEffect hooks remain
    - All useCallback hooks remain
    - fetchAds, fetchInitial, toggleSaveAd, toggleFormat, clearAllFilters all remain

    **Result:** page.tsx should be roughly 350-450 lines — state + effects + slim JSX composing imported components.

    CRITICAL: Do NOT change any behavior, styling, or class names. This is a pure refactor — move code, don't change code.
  </action>
  <verify>
    1. `npx tsc --noEmit` passes
    2. Run `npm run dev` and visit http://localhost:3000/dashboard/v2/ad-library
    3. Verify: stats bar shows 4 cards, filter bar has all dropdowns, ads grid loads, pagination works, search works, save-ad heart button works, dark mode toggle works
    4. Count lines: `wc -l src/app/dashboard/v2/ad-library/page.tsx` should be roughly 350-450
  </verify>
  <done>page.tsx is reduced from ~1044 lines to ~350-450 lines. All functionality identical. Components imported from extracted files.</done>
</task>

<task type="auto">
  <name>Task 2: Update brand detail page to share AdCard</name>
  <files>src/app/dashboard/v2/ad-library/[pageId]/page.tsx</files>
  <action>
    The brand detail page (`[pageId]/page.tsx`) has its own `BrandAdCard` component (lines 371-508) that is nearly identical to `AdCard` from the main page. The key differences:
    - BrandAdCard does NOT show brand name/link (since you're already on the brand page)
    - BrandAdCard does NOT have save functionality
    - BrandAdCard shows "Started" date instead of "Platform"
    - BrandAdCard shows 3-line clamp instead of 2-line clamp for body text

    These differences are meaningful enough that we should NOT force-merge them into a single component. Instead:

    1. Import the shared types from the parent types.ts where applicable
    2. Remove the duplicated `formatFormatLabel` helper (import from `../types` instead)
    3. Keep `BrandAdCard` as a local component in this file — it serves a different purpose than `AdCard`
    4. Import `PaginationData` type if the pagination interface matches, otherwise keep local types

    Specifically:
    - Remove the local `formatFormatLabel` function (line 79-82) and import from `../types`
    - Keep local types (`SerializedBrand`, `SerializedAd`, `SerializedAsset`, `BrandDetailResponse`) as they match a DIFFERENT API response shape than the main page types
    - Keep `BrandAdCard` local — it's intentionally different from `AdCard`
    - Keep `formatDate` local — only used here

    This is a MINIMAL change. Do not restructure the brand page — just eliminate the one duplicated helper.
  </action>
  <verify>
    1. `npx tsc --noEmit` passes
    2. Run `npm run dev` and visit http://localhost:3000/dashboard/v2/ad-library then click on any brand
    3. Brand detail page loads, shows brand header, ad grid, pagination — all working
  </verify>
  <done>Brand detail page imports formatFormatLabel from shared types. No duplicated helpers. Page functions identically.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Ad library page at /dashboard/v2/ad-library renders identically to before
- All filters work: industry, format, days active, brand, status toggle, search, hide carousel
- Pagination works: page numbers, prev/next
- Save ad (heart) works: shows login modal if not authenticated
- Dark mode: no regressions
- Responsive: mobile layout unchanged
- Brand detail page at /dashboard/v2/ad-library/[pageId] renders identically
- page.tsx line count reduced from ~1044 to ~350-450
</verification>

<success_criteria>
- page.tsx is under 500 lines (down from 1044)
- AdCard, FilterBar components, StatsBar, Pagination all imported from component files
- Brand detail page shares formatFormatLabel helper
- Zero visual or functional regressions
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/46-component-extraction/46-02-SUMMARY.md`
</output>
