---
phase: 46-component-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/v2/ad-library/types.ts
  - src/app/dashboard/v2/ad-library/components/ad-card.tsx
  - src/app/dashboard/v2/ad-library/components/filter-dropdown.tsx
  - src/app/dashboard/v2/ad-library/components/filter-chip.tsx
  - src/app/dashboard/v2/ad-library/components/stats-bar.tsx
  - src/app/dashboard/v2/ad-library/components/pagination.tsx
autonomous: true

must_haves:
  truths:
    - "All shared types are importable from a single types.ts file"
    - "AdCard, FilterDropdown, FilterChip, StatsBar, Pagination exist as standalone component files"
    - "Each component compiles without errors"
  artifacts:
    - path: "src/app/dashboard/v2/ad-library/types.ts"
      provides: "Shared types: Ad, AdLibraryStats, TopBrand, Pagination, FilterOption, DaysRange"
      contains: "interface Ad"
    - path: "src/app/dashboard/v2/ad-library/components/ad-card.tsx"
      provides: "AdCard component extracted from page.tsx"
      exports: ["AdCard"]
    - path: "src/app/dashboard/v2/ad-library/components/filter-dropdown.tsx"
      provides: "FilterDropdown component"
      exports: ["FilterDropdown"]
    - path: "src/app/dashboard/v2/ad-library/components/filter-chip.tsx"
      provides: "FilterChip component"
      exports: ["FilterChip"]
    - path: "src/app/dashboard/v2/ad-library/components/stats-bar.tsx"
      provides: "StatsBar component (the 4 stat cards grid)"
      exports: ["StatsBar"]
    - path: "src/app/dashboard/v2/ad-library/components/pagination.tsx"
      provides: "Pagination component with page number generation"
      exports: ["AdPagination"]
  key_links:
    - from: "src/app/dashboard/v2/ad-library/components/ad-card.tsx"
      to: "src/app/dashboard/v2/ad-library/types.ts"
      via: "import { Ad } from '../types'"
      pattern: "import.*Ad.*from.*types"
    - from: "src/app/dashboard/v2/ad-library/components/stats-bar.tsx"
      to: "src/app/dashboard/v2/ad-library/types.ts"
      via: "import { AdLibraryStats } from '../types'"
      pattern: "import.*AdLibraryStats.*from.*types"
---

<objective>
Extract shared types, helpers, and leaf components (AdCard, FilterDropdown, FilterChip, StatsBar, Pagination) from the 1044-line ad library monolith into individual files.

Purpose: Create the component files that Plan 02 will wire back into the main page. This plan creates files only — it does NOT modify page.tsx yet.
Output: 6 new files (1 types file + 5 component files) in the ad-library directory.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/dashboard/v2/ad-library/page.tsx
@src/app/dashboard/v2/v2-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared types file and leaf components</name>
  <files>
    src/app/dashboard/v2/ad-library/types.ts
    src/app/dashboard/v2/ad-library/components/ad-card.tsx
    src/app/dashboard/v2/ad-library/components/filter-dropdown.tsx
    src/app/dashboard/v2/ad-library/components/filter-chip.tsx
  </files>
  <action>
    Create the `components/` directory under `src/app/dashboard/v2/ad-library/`.

    **types.ts** — Extract ALL shared types from page.tsx lines 34-100:
    - `AdLibraryStats`, `TopBrand`, `Ad`, `Pagination` (rename to `PaginationData` to avoid collision with the component name), `FilterOption`, `DaysRange`
    - Also export the `formatFormatLabel` helper function (used by both AdCard and BrandAdCard)
    - Do NOT add 'use client' — this is a pure types/helpers file

    **components/ad-card.tsx** — Extract from page.tsx lines 811-958:
    - Add 'use client' at top
    - Import `Ad` from `../types`, import icons from lucide-react, import Link from next/link, import `formatNumber` from `../../v2-shell`, import `formatFormatLabel` from `../types`
    - Export the `AdCard` component with the same props interface: `{ ad: Ad; darkMode: boolean; isSaved?: boolean; onToggleSave?: (adId: string) => void }`
    - Copy the component EXACTLY as-is — same markup, same logic, same classes

    **components/filter-dropdown.tsx** — Extract from page.tsx lines 965-1015:
    - Add 'use client' at top
    - Import `ChevronDown` from lucide-react
    - Export `FilterDropdown` with same props: `{ label, icon, isOpen, onToggle, onClose, hasValue, darkMode, children }`
    - Copy component exactly as-is

    **components/filter-chip.tsx** — Extract from page.tsx lines 1021-1044:
    - Add 'use client' at top
    - Import `X` from lucide-react
    - Export `FilterChip` with same props: `{ label, onRemove, darkMode }`
    - Copy component exactly as-is
  </action>
  <verify>Run `npx tsc --noEmit` — all 4 new files should compile without errors. Existing page.tsx should still compile since we haven't modified it.</verify>
  <done>types.ts exports all 6 interfaces + formatFormatLabel. AdCard, FilterDropdown, FilterChip exist as standalone components with correct imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create StatsBar and Pagination components</name>
  <files>
    src/app/dashboard/v2/ad-library/components/stats-bar.tsx
    src/app/dashboard/v2/ad-library/components/pagination.tsx
  </files>
  <action>
    **components/stats-bar.tsx** — Extract the stats grid from page.tsx lines 368-392:
    - Add 'use client' at top
    - Import `AdLibraryStats` from `../types`, import `{ Users, BookOpen, Activity, Archive }` from lucide-react, import `{ V2Card, formatNumber }` from `../../v2-shell`
    - Props: `{ stats: AdLibraryStats | null; darkMode: boolean }`
    - The component renders the `statCards` array and the grid of 4 V2Cards
    - Include the `statCards` definition inside the component (the array with label, value, icon, color)
    - Copy markup exactly from lines 380-392

    **components/pagination.tsx** — Extract from page.tsx lines 649-698 + helper 789-805:
    - Add 'use client' at top
    - Import `ChevronLeft, ChevronRight` from lucide-react
    - Include the `generatePageNumbers` helper function in this file (not exported, just module-level)
    - Export `AdPagination` component (named to avoid collision with the type)
    - Props: `{ page: number; totalPages: number; onPageChange: (page: number) => void; darkMode: boolean }`
    - The component renders the prev/next buttons and page number buttons
    - `onPageChange` replaces the direct `setPage` calls. Map: prev button calls `onPageChange(Math.max(1, page - 1))`, page buttons call `onPageChange(p as number)`, next button calls `onPageChange(Math.min(totalPages, page + 1))`
    - Include the `totalPages > 1` guard inside the component — if totalPages <= 1, return null
    - Copy markup exactly, just replacing setPage with onPageChange
  </action>
  <verify>Run `npx tsc --noEmit` — all new files compile. Run `npm run dev` and confirm the existing page still loads (we haven't changed it yet).</verify>
  <done>StatsBar and AdPagination components exist as standalone files with correct props and identical markup to the original.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 6 new files exist under `src/app/dashboard/v2/ad-library/`
- The existing page.tsx is NOT modified (still works as-is)
- Each component file has 'use client' directive (except types.ts)
- Each component imports types from ../types (not redeclaring them)
</verification>

<success_criteria>
- 6 new files created: types.ts + 5 component files
- All files compile without TypeScript errors
- Components match the original markup exactly (pixel-perfect extraction)
- No modifications to page.tsx yet — app still runs identically
</success_criteria>

<output>
After completion, create `.planning/phases/46-component-extraction/46-01-SUMMARY.md`
</output>
