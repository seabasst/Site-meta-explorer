---
phase: 56-image-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/v2/creative-lab/format-selector.tsx
  - src/app/dashboard/v2/creative-lab/generation-results.tsx
  - src/app/dashboard/v2/creative-lab/page.tsx
autonomous: false

must_haves:
  truths:
    - "User can click Generate on a recommendation and see a format selection panel"
    - "User can select one or more ad formats (square, story, landscape, portrait, etc.)"
    - "User can edit the pre-filled prompt before generating"
    - "User can generate images for all selected formats in parallel"
    - "User can see a grid of generated images with format labels"
    - "User can download any generated image reliably (cross-origin safe)"
    - "User can go back to results and generate from a different recommendation"
  artifacts:
    - path: "src/app/dashboard/v2/creative-lab/format-selector.tsx"
      provides: "Format selection modal with AD_FORMATS constant, prompt editing, multi-select"
      exports: ["FormatSelector", "AD_FORMATS", "AdFormat"]
    - path: "src/app/dashboard/v2/creative-lab/generation-results.tsx"
      provides: "Multi-image result grid with per-image download, regenerate, loading states"
      exports: ["GenerationResults", "GenerationResult"]
    - path: "src/app/dashboard/v2/creative-lab/page.tsx"
      provides: "Updated step machine integrating format selection and multi-format results"
      contains: "format-select"
  key_links:
    - from: "format-selector.tsx"
      to: "page.tsx"
      via: "onGenerate callback with selected formats and edited prompt"
      pattern: "onGenerate.*AdFormat"
    - from: "page.tsx"
      to: "/api/analyze/generate-image"
      via: "Promise.allSettled for each selected format"
      pattern: "Promise\\.allSettled"
    - from: "generation-results.tsx"
      to: "blob download"
      via: "fetch-to-blob pattern for cross-origin safe downloads"
      pattern: "URL\\.createObjectURL"
---

<objective>
Add multi-format image generation to Creative Lab with format selection, parallel generation, and reliable downloads.

Purpose: Users need to generate ad creatives in specific ad placement sizes (story, feed, landscape, etc.) from their analysis recommendations. The generation API already works but is hardcoded to 1:1 square.

Output: Two new components (FormatSelector, GenerationResults) wired into the existing Creative Lab page, replacing the single-image generation flow with a multi-format workflow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-image-generation/56-RESEARCH.md
@src/app/dashboard/v2/creative-lab/page.tsx
@src/app/api/analyze/generate-image/route.ts
@src/app/dashboard/v2/v2-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FormatSelector and GenerationResults components</name>
  <files>
    src/app/dashboard/v2/creative-lab/format-selector.tsx
    src/app/dashboard/v2/creative-lab/generation-results.tsx
  </files>
  <action>
Create two new components:

**format-selector.tsx:**

1. Export `AD_FORMATS` constant array with these formats:
   - Square: 1080x1080, aspect ratio '1:1', "Feed post, Instagram"
   - Story/Reel: 1080x1920, aspect ratio '9:16', "Stories, Reels, TikTok"
   - Landscape: 1200x628, aspect ratio '16:9', "Facebook feed link ad"
   - Portrait: 1080x1350, aspect ratio '4:5', "Instagram feed, Facebook"
   - Wide Banner: 1920x800, aspect ratio '21:9', "Display banner"
   - Pinterest Pin: 1000x1500, aspect ratio '2:3', "Pinterest"
   Each format object: `{ id: string, label: string, size: string, aspectRatio: string, description: string }`

2. Export `AdFormat` type for the format object.

3. Export `FormatSelector` component with props:
   - `recommendation`: the selected Recommendation (with briefTitle, imagePrompt, suggestion)
   - `darkMode`: boolean
   - `onGenerate`: `(formats: AdFormat[], prompt: string) => void`
   - `onBack`: `() => void`

4. Component renders:
   - Back button (ArrowLeft icon, calls onBack)
   - Brief summary card showing recommendation briefTitle and suggestion
   - Editable prompt textarea, pre-filled with `recommendation.imagePrompt`, labeled "Image Prompt (editable)"
   - Format grid: 2-column grid of format cards, each showing label, size, description, with a checkbox/toggle. Clicking toggles selection (blue border + checkmark when selected). Square selected by default.
   - "Select All" / "Deselect All" toggle
   - "Generate {N} Image(s)" button (disabled if none selected), calls onGenerate with selected formats and current prompt text

5. Styling: Use the project's design system -- `#1235e2` primary, dark/light mode via `darkMode` prop. Use V2Card for card containers. Match the rounded-xl, border styling from page.tsx.

**generation-results.tsx:**

1. Export `GenerationResult` type: `{ format: AdFormat, imageUrl: string | null, status: 'loading' | 'success' | 'error', error?: string }`

2. Export `GenerationResults` component with props:
   - `results`: `GenerationResult[]`
   - `prompt`: string (the prompt that was used)
   - `recommendation`: Recommendation object
   - `darkMode`: boolean
   - `onBack`: `() => void` (back to results)
   - `onRegenerate`: `(format: AdFormat) => void` (regenerate single format)

3. Component renders:
   - Back button to results
   - Header showing recommendation briefTitle
   - Responsive grid of result cards (2 cols on md, 1 on mobile):
     - Loading state: skeleton shimmer with Loader2 spinner and format label
     - Success state: image with format label badge, size info, download button, regenerate button
     - Error state: error message with retry button
   - Download button uses fetch-to-blob pattern (NOT `<a href download>`):
     ```
     async function downloadImage(url: string, filename: string) {
       const response = await fetch(url);
       const blob = await response.blob();
       const objectUrl = URL.createObjectURL(blob);
       const a = document.createElement('a');
       a.href = objectUrl;
       a.download = filename;
       document.body.appendChild(a);
       a.click();
       document.body.removeChild(a);
       URL.revokeObjectURL(objectUrl);
     }
     ```
   - Download filename: `ad-creative-{format.id}.webp`
   - "Download All" button when 2+ images are successful (downloads each sequentially with 200ms delay to avoid browser blocking)

4. Styling: Same design system. Loading skeleton should use animate-pulse. Format label badge positioned over image top-left.
  </action>
  <verify>
    Run `npx tsc --noEmit` and confirm no type errors in the two new files. Visually inspect that both components export the expected names.
  </verify>
  <done>
    FormatSelector renders format grid with multi-select, editable prompt, and generate button.
    GenerationResults renders a responsive grid with loading/success/error states and blob-based download.
    Both components type-check cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire format selection and multi-format generation into page.tsx</name>
  <files>
    src/app/dashboard/v2/creative-lab/page.tsx
  </files>
  <action>
Modify the Creative Lab page to use the new components:

1. **Update imports:** Add `FormatSelector`, `AD_FORMATS`, `AdFormat` from `./format-selector` and `GenerationResults`, `GenerationResult` from `./generation-results`.

2. **Update Step type** (line 189): Change to:
   `type Step = 'setup' | 'analyzing' | 'results' | 'brief' | 'format-select' | 'generating-image' | 'image-result';`

3. **Add state variables** (near line 268):
   - `const [generationResults, setGenerationResults] = useState<GenerationResult[]>([]);`
   - `const [generationPrompt, setGenerationPrompt] = useState('');`
   Remove or keep `generatedImageUrl` state (it's no longer used for the main flow, but keep it if the brief view still references it -- check and clean up).

4. **Replace `generateImage` function** (lines 361-380) with two functions:

   a. `handleGenerateClick(rec: Recommendation)` -- called when user clicks "Generate" on a recommendation card:
      - Sets `selectedRec` to rec
      - Sets step to `'format-select'`

   b. `handleGenerate(formats: AdFormat[], prompt: string)` -- called from FormatSelector's onGenerate:
      - Sets `generationPrompt` to prompt
      - Sets step to `'generating-image'`
      - Initializes `generationResults` with one entry per format, all status `'loading'`
      - Fires parallel requests using `Promise.allSettled`:
        ```
        formats.forEach(async (format) => {
          try {
            const res = await fetch('/api/analyze/generate-image', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ prompt, aspectRatio: format.aspectRatio }),
            });
            if (!res.ok) throw new Error('Generation failed');
            const data = await res.json();
            setGenerationResults(prev => prev.map(r =>
              r.format.id === format.id ? { ...r, imageUrl: data.imageUrl, status: 'success' as const } : r
            ));
          } catch {
            setGenerationResults(prev => prev.map(r =>
              r.format.id === format.id ? { ...r, status: 'error' as const, error: 'Generation failed' } : r
            ));
          }
        });
        ```
      - After ALL requests settle, set step to `'image-result'`
      - Use a counter or check: when all results have status !== 'loading', transition to 'image-result'. Actually, since results update progressively, go directly to 'image-result' step and let the GenerationResults component show loading/success/error states as they resolve. So: set step to 'image-result' immediately after firing requests (NOT after waiting).

   c. `handleRegenerateSingle(format: AdFormat)` -- called from GenerationResults' onRegenerate:
      - Sets that format's result back to loading
      - Fires a single request for that format
      - Updates the result on completion

5. **Update "Generate" button on recommendation cards** (line 1047): Change `onClick={() => generateImage(rec)}` to `onClick={() => handleGenerateClick(rec)}`

6. **Update the brief view's "Generate AI Image" button** (around line 570-580 in the brief step): Also change to `onClick={() => handleGenerateClick(selectedRec!)}` so it goes through format selection too.

7. **Add format-select step rendering** (between brief and generating-image steps, around line 590):
   ```
   if (step === 'format-select' && selectedRec) {
     return (
       <V2Shell title="Creative Lab">
         <div className="max-w-3xl mx-auto">
           <FormatSelector
             recommendation={selectedRec}
             darkMode={darkMode}
             onGenerate={handleGenerate}
             onBack={() => { setStep('results'); setSelectedRec(null); }}
           />
         </div>
       </V2Shell>
     );
   }
   ```

8. **Update generating-image step** (lines 594-603): Keep the loading spinner but update text to "Generating {generationResults.length} variant(s)...". Actually, remove this step entirely since we go straight to image-result and show progressive loading there.

   OR simpler: skip the 'generating-image' step. In `handleGenerate`, set step directly to `'image-result'` and let the results component handle the loading state per format. This eliminates the intermediate loading screen.

9. **Replace image-result step** (lines 606-640) with:
   ```
   if (step === 'image-result' && selectedRec) {
     return (
       <V2Shell title="Creative Lab">
         <div className="max-w-4xl mx-auto">
           <GenerationResults
             results={generationResults}
             prompt={generationPrompt}
             recommendation={selectedRec}
             darkMode={darkMode}
             onBack={() => { setStep('results'); setSelectedRec(null); setGenerationResults([]); }}
             onRegenerate={handleRegenerateSingle}
           />
         </div>
       </V2Shell>
     );
   }
   ```

10. **Update resetToStart** (line 382): Add `setGenerationResults([])` and `setGenerationPrompt('')`.

11. **Clean up unused state:** If `generatedImageUrl` is no longer referenced anywhere, remove it and its setter.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- no type errors.
    Run `npm run build` -- builds successfully.
    Start dev server and navigate to `/dashboard/v2/creative-lab`. Select a brand, run analysis, click "Generate" on a recommendation. Confirm format selector appears. Select formats, click generate, confirm images appear progressively in the results grid. Click download on an image and confirm it downloads as a .webp file (not opening in new tab).
  </verify>
  <done>
    - Clicking "Generate" on a recommendation shows format selector (GENR-01, GENR-05)
    - Format selector shows 6 ad formats with multi-select (GENR-02)
    - Prompt is editable and pre-filled from recommendation (GENR-05)
    - Multiple formats generate in parallel with progressive loading (GENR-03)
    - Download uses blob pattern and saves file reliably (GENR-04)
    - Back navigation works throughout the flow
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Multi-format image generation flow in Creative Lab:
    1. Format selector with 6 ad sizes, editable prompt, multi-select
    2. Parallel generation with progressive loading
    3. Results grid with per-image download (blob-based)
    4. Full flow: recommendation -> format select -> generate -> view/download
  </what-built>
  <how-to-verify>
    1. Go to https://localhost:3000/dashboard/v2/creative-lab (or your dev URL)
    2. Search for a brand and run analysis
    3. On the results page, click "Generate" on any recommendation card
    4. Verify: format selector appears with 6 format options, prompt is pre-filled
    5. Edit the prompt slightly, select 2-3 formats (e.g., Square + Story + Landscape)
    6. Click "Generate 3 Image(s)"
    7. Verify: results grid shows loading states that progressively resolve to images
    8. Verify: each image has correct format label and size info
    9. Click "Download" on one image -- verify it saves as a .webp file (not opening in new tab)
    10. Click "Back to results" -- verify you return to the analysis results
    11. Test the "Brief" button -> "Generate AI Image" button path too -- should also go through format selection
    12. Test dark mode toggle -- verify both components look correct in both modes
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` completes successfully
- Format selector renders with all 6 formats and editable prompt
- Multi-format generation fires parallel API calls (check Network tab)
- Generated images display in responsive grid with format labels
- Download saves .webp file via blob pattern (not opening in new tab)
- Back navigation returns to results without stale state
- Dark mode and light mode both render correctly
</verification>

<success_criteria>
All five GENR requirements are met:
- GENR-01: Generate AI images from recommendations (click Generate -> format select -> generate)
- GENR-02: Select target ad format/size (6 formats with aspect ratio mapping)
- GENR-03: Generate multiple format variants from single prompt (parallel Promise.allSettled)
- GENR-04: Download generated images (blob-based cross-origin safe download)
- GENR-05: Prompts pre-filled from analysis gaps (recommendation.imagePrompt -> editable textarea)
</success_criteria>

<output>
After completion, create `.planning/phases/56-image-generation/56-01-SUMMARY.md`
</output>
