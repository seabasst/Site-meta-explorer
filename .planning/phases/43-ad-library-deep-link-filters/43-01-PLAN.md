---
phase: 43-ad-library-deep-link-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/v2/ad-library/page.tsx
  - src/components/dashboard/top-brands-table.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Navigating to /dashboard/v2/ad-library?brandPageId=123 pre-fills the brand filter and shows filtered results"
    - "TopBrandsTable links use brandPageId param (not brand)"
    - "Category Detail View Ads links land on Ad Library with brand filter applied"
  artifacts:
    - path: "src/app/dashboard/v2/ad-library/page.tsx"
      provides: "URL search param hydration for brandPageId"
      contains: "useSearchParams"
    - path: "src/components/dashboard/top-brands-table.tsx"
      provides: "Correct deep-link param name"
      contains: "brandPageId="
  key_links:
    - from: "src/components/dashboard/top-brands-table.tsx"
      to: "src/app/dashboard/v2/ad-library/page.tsx"
      via: "URL param brandPageId"
      pattern: "brandPageId="
    - from: "src/app/dashboard/v2/categories/[slug]/page.tsx"
      to: "src/app/dashboard/v2/ad-library/page.tsx"
      via: "URL param brandPageId"
      pattern: "brandPageId="
---

<objective>
Hydrate Ad Library filters from URL search params and fix TopBrandsTable param name so drill-downs from Dashboard and Category Detail pages work end-to-end.

Purpose: Closes the integration gap between Phase 40 (Dashboard TopBrandsTable) and Phase 38 (Category Detail) — both link to the Ad Library with a brand filter, but the Ad Library page ignores URL params.
Output: Two files updated; deep-link filtering works from both entry points.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/dashboard/v2/ad-library/page.tsx
@src/components/dashboard/top-brands-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hydrate brandFilter from URL search params on Ad Library page</name>
  <files>src/app/dashboard/v2/ad-library/page.tsx</files>
  <action>
    In `AdLibraryPage` component:

    1. Import `useSearchParams` from `next/navigation`.
    2. Call `const searchParams = useSearchParams()` near the top of the component (after useV2).
    3. Read the `brandPageId` param: `const urlBrandPageId = searchParams.get('brandPageId') || ''`.
    4. Change the `brandFilter` useState initializer from `''` to `urlBrandPageId`:
       ```
       const [brandFilter, setBrandFilter] = useState<string>(urlBrandPageId);
       ```
    5. Do NOT add a useEffect that re-syncs on searchParams change — the initial hydration is sufficient. The user can then change filters via the UI and the URL does not need to stay in sync (no router.push needed).

    This is the minimal fix: read param once on mount, seed the filter state, let existing fetchAds logic handle the rest (it already passes brandFilter as `brandPageId` to the API on line 228).
  </action>
  <verify>
    - `grep -n 'useSearchParams' src/app/dashboard/v2/ad-library/page.tsx` shows the import and usage
    - `grep -n "searchParams.get('brandPageId')" src/app/dashboard/v2/ad-library/page.tsx` shows param reading
    - Run `npx next build` or `npx tsc --noEmit` to confirm no type errors
  </verify>
  <done>
    Opening `/dashboard/v2/ad-library?brandPageId=SOME_PAGE_ID` pre-fills the brand filter and the initial API call includes that brandPageId, showing filtered results immediately.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix TopBrandsTable param name from brand to brandPageId</name>
  <files>src/components/dashboard/top-brands-table.tsx</files>
  <action>
    On line 116, change the Link href from:
    ```
    href={`/dashboard/v2/ad-library?brand=${brand.pageId}`}
    ```
    to:
    ```
    href={`/dashboard/v2/ad-library?brandPageId=${brand.pageId}`}
    ```

    This aligns with:
    - The Ad Library page now reading `brandPageId` from search params (Task 1)
    - The Category Detail page already using `brandPageId` (line 332 of categories/[slug]/page.tsx)
    - The ads API already accepting `brandPageId` as the query param name
  </action>
  <verify>
    - `grep 'ad-library?brand=' src/components/dashboard/top-brands-table.tsx` returns NO results (old param gone)
    - `grep 'ad-library?brandPageId=' src/components/dashboard/top-brands-table.tsx` returns the updated link
  </verify>
  <done>
    Clicking a brand name in the Dashboard TopBrandsTable navigates to `/dashboard/v2/ad-library?brandPageId=XXXX` which the Ad Library page now correctly reads and filters by.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Navigate to `/dashboard/v2/ad-library?brandPageId=KNOWN_PAGE_ID` — brand filter is pre-filled and ads are filtered
3. On Dashboard, click a brand in TopBrandsTable — URL uses `brandPageId` param and Ad Library shows filtered results
4. On Category Detail page, click "View Ads" for a brand — Ad Library shows filtered results (this already uses correct param name, just verifying end-to-end)
</verification>

<success_criteria>
- All three success criteria from the roadmap are met:
  1. `/dashboard/v2/ad-library?brandPageId=123` pre-fills brand filter and shows filtered results
  2. TopBrandsTable links use `brandPageId` (not `brand`)
  3. Category Detail "View Ads" links work end-to-end with brand filter applied
- No TypeScript errors
- No regressions to existing Ad Library filter functionality
</success_criteria>

<output>
After completion, create `.planning/phases/43-ad-library-deep-link-filters/43-01-SUMMARY.md`
</output>
