---
phase: 47-filter-sort-overhaul
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - src/app/dashboard/v2/ad-library/components/filter-bar.tsx
  - src/app/dashboard/v2/ad-library/components/filter-chip.tsx
  - src/app/dashboard/v2/ad-library/components/ad-card.tsx
  - src/app/dashboard/v2/ad-library/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can select a sort option from a dropdown showing Reach, Spend, Days Active, Start Date, Date Added"
    - "User can toggle a partnership filter to show only partnership ads or only non-partnership ads"
    - "Active filter chips are visually prominent with clear X removal buttons"
    - "Filter bar sticks to top of viewport when user scrolls past it"
    - "User can toggle between compact and standard grid density"
  artifacts:
    - path: "src/app/dashboard/v2/ad-library/components/filter-bar.tsx"
      provides: "Complete FilterBar component with sort, filters, partnership, density, sticky"
      min_lines: 150
    - path: "src/app/dashboard/v2/ad-library/components/filter-chip.tsx"
      provides: "Improved FilterChip with better visual treatment"
      min_lines: 15
    - path: "src/app/dashboard/v2/ad-library/components/ad-card.tsx"
      provides: "AdCard with optional compact prop for density toggle"
      contains: "compact"
    - path: "src/app/dashboard/v2/ad-library/page.tsx"
      provides: "Page using FilterBar component with sort/density/partnership state"
      contains: "FilterBar"
  key_links:
    - from: "src/app/dashboard/v2/ad-library/components/filter-bar.tsx"
      to: "src/app/dashboard/v2/ad-library/types.ts"
      via: "imports SortField, SORT_OPTIONS, GridDensity"
      pattern: "import.*SortField.*SORT_OPTIONS.*GridDensity"
    - from: "src/app/dashboard/v2/ad-library/page.tsx"
      to: "src/app/dashboard/v2/ad-library/components/filter-bar.tsx"
      via: "renders FilterBar with state props"
      pattern: "<FilterBar"
    - from: "src/app/dashboard/v2/ad-library/page.tsx"
      to: "/api/ad-library/ads"
      via: "fetch with sortBy, hasBylines params"
      pattern: "sortBy|hasBylines"
---

<objective>
Extract the inline filter bar into a FilterBar component and add sort controls, partnership filter, grid density toggle, sticky behavior, and improved filter chips.

Purpose: This is the core frontend work of Phase 47 — transforming the filter bar from a basic set of dropdowns into a complete, sticky control surface with sort, filter, and view options.
Output: New FilterBar component, improved FilterChip, and updated page.tsx wired to new state.

Note: List view (BRWS-03 "optional list view") is descoped from this plan. The GridDensity type supports 'standard' and 'compact' only. List view can be added as a future enhancement if needed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/47-filter-sort-overhaul/47-01-SUMMARY.md
@src/app/dashboard/v2/ad-library/page.tsx
@src/app/dashboard/v2/ad-library/types.ts
@src/app/dashboard/v2/ad-library/components/filter-dropdown.tsx
@src/app/dashboard/v2/ad-library/components/filter-chip.tsx
@src/app/dashboard/v2/ad-library/components/ad-card.tsx
@src/app/dashboard/v2/v2-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FilterBar component with all controls</name>
  <files>src/app/dashboard/v2/ad-library/components/filter-bar.tsx, src/app/dashboard/v2/ad-library/components/filter-chip.tsx</files>
  <action>
  **Create `filter-bar.tsx`** — a new component that replaces the inline filter bar (page.tsx lines 293-503). The component receives state and callbacks as props, not internal state.

  Props interface (define in the file):
  ```
  interface FilterBarProps {
    darkMode: boolean;
    // Search
    searchQuery: string;
    onSearchChange: (query: string) => void;
    // Status
    statusFilter: 'active' | 'all';
    onStatusChange: (status: 'active' | 'all') => void;
    // Sort
    sortBy: SortField;
    sortOrder: 'asc' | 'desc';
    onSortByChange: (field: SortField) => void;
    onSortOrderToggle: () => void;
    // Filters
    categoryFilter: string;
    onCategoryChange: (cat: string) => void;
    categories: FilterOption[];
    selectedFormats: Set<string>;
    onToggleFormat: (fmt: string) => void;
    formatOptions: FilterOption[];
    daysActiveFilter: DaysRange | null;
    onDaysActiveChange: (range: DaysRange | null) => void;
    daysRanges: DaysRange[];
    brandFilter: string;
    onBrandChange: (brand: string) => void;
    topBrands: TopBrand[];
    hideCarousel: boolean;
    onHideCarouselToggle: () => void;
    // Partnership
    partnershipFilter: 'all' | 'partnership' | 'non-partnership';
    onPartnershipChange: (filter: 'all' | 'partnership' | 'non-partnership') => void;
    // Grid density
    gridDensity: GridDensity;
    onGridDensityChange: (density: GridDensity) => void;
    // Result count
    resultCount: number | null;
    // Clear
    activeFilterCount: number;
    onClearAll: () => void;
  }
  ```

  **Layout — 3 rows:**

  **Row 1:** Search input (left, flex-1 min-w-[200px]) | Active/All toggle | Sort dropdown + sort order toggle button | Grid density toggle (compact/standard icons) | Result count (right)

  The sort dropdown: Use the FilterDropdown component. Label shows current sort label (e.g. "Reach"). Icon: `ArrowUpDown` from lucide. Dropdown lists SORT_OPTIONS with radio-style selection (active item highlighted with #1235e2). Next to the dropdown, a small button toggles asc/desc — show `ArrowUp` or `ArrowDown` icon from lucide.

  Grid density toggle: Two small icon buttons side by side in a pill container (similar to Active/All toggle style). Use `Grid3x3` icon for standard, `LayoutGrid` for compact (both from lucide). Active state uses the same style as the Active/All toggle (bg-[#1235e2] text-white in dark, bg-white text-[#1235e2] shadow in light).

  **Row 2:** Industry dropdown | Format dropdown | Days Active dropdown | Brand dropdown | Partnership filter (3-state toggle: All / Partnership / Non-partnership — use a segmented control like Active/All but with 3 options, using `Users` icon) | Hide Carousel toggle

  **Row 3 (conditional, only if activeFilterCount > 0):** Divider line | Active filter chips | "Clear all" link

  Include partnership filter chip when partnershipFilter !== 'all'. Include sort chip when sortBy !== 'reachEstimate' (since reach is the default).

  **Sticky behavior:** Wrap the entire FilterBar in a div with: `sticky top-0 z-30` (z-30 to sit below modals at z-50). Add a subtle bottom border/shadow when stuck. Use a simple approach: always apply `sticky top-0 z-30` to the wrapper. The V2Card already has a background, so sticking will work naturally. Add `shadow-md` class to give visual separation when sticky.

  Match existing design system: #1235e2 primary, dark (#101322) / light (#f6f6f8) modes, existing button/dropdown patterns from the current inline filter bar.

  **Update `filter-chip.tsx`:** Improve the chip visual treatment:
  - Add a subtle left-side colored border (2px solid #1235e2) for more visual prominence
  - Increase padding slightly: `px-3 py-1.5` (was `px-2.5 py-1`)
  - Add hover state on the X button: `hover:bg-red-500/20 rounded-full p-0.5`
  - Keep the existing color scheme (bg-[#1235e2]/20 text-[#1235e2] in dark, bg-[#1235e2]/10 in light)
  </action>
  <verify>
  Run: `npx tsc --noEmit --pretty 2>&1 | head -20` — no type errors
  Verify file exists: `wc -l src/app/dashboard/v2/ad-library/components/filter-bar.tsx` — should be 150+ lines
  </verify>
  <done>FilterBar component created with sort dropdown, partnership 3-state toggle, grid density toggle, sticky wrapper, and all existing filter controls. FilterChip has improved visual treatment.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FilterBar into page.tsx with new state</name>
  <files>src/app/dashboard/v2/ad-library/page.tsx, src/app/dashboard/v2/ad-library/components/ad-card.tsx</files>
  <action>
  Replace the inline filter bar (the V2Card from line 293 to line 503) with the new FilterBar component. Add new state variables and wire them to the API fetch.

  1. **Add new state variables** (after existing filter state around line 76):
     ```
     const [sortBy, setSortBy] = useState<SortField>('reachEstimate');
     const [sortOrder, setSortOrder] = useState<'asc' | 'desc'>('desc');
     const [partnershipFilter, setPartnershipFilter] = useState<'all' | 'partnership' | 'non-partnership'>('all');
     const [gridDensity, setGridDensity] = useState<GridDensity>('standard');
     ```
     Import `SortField`, `GridDensity` from `./types`.

  2. **Update fetchAds** to use dynamic sort and partnership filter:
     - Change hardcoded `sortBy: 'reachEstimate'` (line 134) to use the `sortBy` state variable
     - Change hardcoded `sortOrder: 'desc'` (line 135) to use the `sortOrder` state variable
     - Add partnership filter param: if `partnershipFilter === 'partnership'`, set `params.set('hasBylines', 'true')`; if `partnershipFilter === 'non-partnership'`, set `params.set('hasBylines', 'false')`
     - Add `sortBy`, `sortOrder`, and `partnershipFilter` to the useCallback dependency array

  3. **Update the reset-to-page-1 effect** (line 199-201) to include `sortBy`, `sortOrder`, `partnershipFilter` in the dependency array.

  4. **Update activeFilterCount** to include `(partnershipFilter !== 'all' ? 1 : 0)`.

  5. **Update clearAllFilters** to reset `setPartnershipFilter('all')`, `setSortBy('reachEstimate')`, `setSortOrder('desc')`.

  6. **Replace the inline filter V2Card** (lines 293-503) with:
     ```
     <FilterBar
       darkMode={darkMode}
       searchQuery={searchQuery}
       onSearchChange={setSearchQuery}
       statusFilter={statusFilter}
       onStatusChange={setStatusFilter}
       sortBy={sortBy}
       sortOrder={sortOrder}
       onSortByChange={setSortBy}
       onSortOrderToggle={() => setSortOrder(o => o === 'desc' ? 'asc' : 'desc')}
       categoryFilter={categoryFilter}
       onCategoryChange={setCategoryFilter}
       categories={categories}
       selectedFormats={selectedFormats}
       onToggleFormat={toggleFormat}
       formatOptions={formatOptions}
       daysActiveFilter={daysActiveFilter}
       onDaysActiveChange={setDaysActiveFilter}
       daysRanges={daysRanges}
       brandFilter={brandFilter}
       onBrandChange={setBrandFilter}
       topBrands={topBrands}
       hideCarousel={hideCarousel}
       onHideCarouselToggle={() => setHideCarousel(h => !h)}
       partnershipFilter={partnershipFilter}
       onPartnershipChange={setPartnershipFilter}
       gridDensity={gridDensity}
       onGridDensityChange={setGridDensity}
       resultCount={pagination?.total ?? null}
       activeFilterCount={activeFilterCount}
       onClearAll={clearAllFilters}
     />
     ```

  7. **Wire grid density to the ad grid** — update the grid classes on the ads grid container (line 540) and loading skeleton (line 519):
     - Standard: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6` (current)
     - Compact: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 xl:grid-cols-6 gap-3`
     Use a ternary: `gridDensity === 'compact' ? 'grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 xl:grid-cols-6 gap-3' : 'grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6'`

  8. **Pass gridDensity to AdCard** as a prop (AdCard will need a `compact` boolean). In compact mode, AdCard should use `aspect-[4/4]` instead of `aspect-[4/5]` for the preview area — update the AdCard component in `ad-card.tsx` to accept an optional `compact?: boolean` prop and conditionally apply the aspect ratio.

  9. **Remove unused imports** — the inline filter bar used several lucide icons directly (Search, Layers, Calendar, Tag, Users). Remove any that are no longer directly used in page.tsx (they'll be used inside FilterBar instead). Keep BookOpen, Heart, LogIn, X which are used elsewhere in the page.

  10. **Add FilterBar import**: `import { FilterBar } from './components/filter-bar';`
  </action>
  <verify>
  Run: `npx tsc --noEmit --pretty 2>&1 | head -20` — no type errors
  Run dev server and verify: the filter bar renders with all controls, sort dropdown works, partnership toggle shows 3 states, density toggle switches grid layout, sticky behavior works on scroll.
  Page line count should decrease (moved ~200 lines of filter UI to FilterBar component).
  `wc -l src/app/dashboard/v2/ad-library/page.tsx` — should be under 500 lines.
  </verify>
  <done>
  - page.tsx uses FilterBar component instead of inline filter markup
  - Sort dropdown controls sortBy/sortOrder state, wired to API fetch
  - Partnership filter sends hasBylines param to API
  - Grid density toggles between standard (4-col) and compact (5-6-col) layouts
  - AdCard renders with reduced aspect ratio in compact mode
  - Filter bar is sticky with z-30
  - All filter chips include partnership and sort when active
  </done>
</task>

</tasks>

<verification>
1. Filter bar renders with: search, status toggle, sort dropdown, density toggle, result count (row 1)
2. Filter dropdowns work: Industry, Format, Days Active, Brand, Partnership toggle, Hide Carousel (row 2)
3. Active filter chips appear with improved styling and clear-all link (row 3)
4. Sorting by each option (Reach, Spend, Days Active, Start Date, Date Added) reorders the grid
5. Toggling sort order (asc/desc) reverses the order
6. Partnership filter: "All" shows all, "Partnership" shows only bylines ads, "Non-partnership" hides bylines ads
7. Grid density: Standard = 4 columns on lg, Compact = 5-6 columns with smaller cards
8. Filter bar sticks to top when scrolling down
9. No regressions in existing filter functionality
10. Dark mode and light mode both render correctly
</verification>

<success_criteria>
- FilterBar is a standalone component in filter-bar.tsx
- page.tsx is under 500 lines (moved filter UI out)
- All 5 sort options work via the API
- Partnership 3-state toggle filters correctly
- Grid density toggle changes layout visually (compact and standard; list view descoped)
- Sticky filter bar stays visible on scroll
- Filter chips show improved visual treatment with easy removal
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/47-filter-sort-overhaul/47-02-SUMMARY.md`
</output>
