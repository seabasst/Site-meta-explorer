---
phase: 47-filter-sort-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/ad-library/ads/route.ts
  - src/app/dashboard/v2/ad-library/types.ts
autonomous: true

must_haves:
  truths:
    - "API accepts sortBy=spendLower and sortBy=adDurationDays and returns correctly ordered results"
    - "API accepts hasBylines=true|false and filters ads by presence/absence of bylines field"
  artifacts:
    - path: "src/app/api/ad-library/ads/route.ts"
      provides: "Extended sort and filter support"
      contains: "spendLower.*adDurationDays"
    - path: "src/app/dashboard/v2/ad-library/types.ts"
      provides: "SortField type for frontend consumption"
      contains: "SortField"
  key_links:
    - from: "src/app/api/ad-library/ads/route.ts"
      to: "prisma.adLibraryAd"
      via: "orderBy clause with new sort fields"
      pattern: "spendLower|adDurationDays"
---

<objective>
Extend the ads API to support sorting by spend and days active, and filtering by partnership (bylines) status.

Purpose: The frontend sort and partnership filter controls (Plan 02) need these backend capabilities to function.
Output: Updated API route with 2 new sort fields and 1 new filter parameter, plus a SortField type export.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/api/ad-library/ads/route.ts
@src/app/dashboard/v2/ad-library/types.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spendLower and adDurationDays sort fields to ads API</name>
  <files>src/app/api/ad-library/ads/route.ts</files>
  <action>
  In `parseQueryParams`:
  1. Update the `validSortFields` array from `['startDate', 'reachEstimate', 'createdAt']` to `['startDate', 'reachEstimate', 'createdAt', 'spendLower', 'adDurationDays']`
  2. Update the `AdLibraryAdSortOptions` interface `sortBy` type to include `'spendLower' | 'adDurationDays'`
  3. Update the type assertion on the sortBy assignment to match the expanded union

  The `buildOrderByClause` function already uses dynamic `[sortBy]` key lookup, so it will work with the new fields automatically. Both `spendLower` (Float?) and `adDurationDays` (Int?) are nullable fields in the Prisma schema, and the existing `{ sort: sortOrder, nulls: 'last' }` pattern handles nulls correctly.
  </action>
  <verify>
  Run: `curl -s "http://localhost:3000/api/ad-library/ads?sortBy=spendLower&sortOrder=desc&limit=3" | jq '.ads[:3] | .[].spendLower'` — should return ads sorted by spend descending with nulls last.
  Run: `curl -s "http://localhost:3000/api/ad-library/ads?sortBy=adDurationDays&sortOrder=desc&limit=3" | jq '.ads[:3] | .[].adDurationDays'` — should return ads sorted by days active descending.
  </verify>
  <done>API accepts sortBy=spendLower and sortBy=adDurationDays, returns correctly ordered results with nulls sorted last</done>
</task>

<task type="auto">
  <name>Task 2: Add hasBylines filter to ads API and export SortField type</name>
  <files>src/app/api/ad-library/ads/route.ts, src/app/dashboard/v2/ad-library/types.ts</files>
  <action>
  In `route.ts`:
  1. Add `hasBylines?: boolean` to the `AdLibraryAdFilters` interface
  2. In `parseQueryParams`, parse the `hasBylines` query param: `const hasBylinesRaw = searchParams.get('hasBylines'); const hasBylines = hasBylinesRaw !== null ? hasBylinesRaw === 'true' : undefined;` — add to returned filters object
  3. In `buildWhereClause`, add bylines filter logic:
     - If `filters.hasBylines === true`: `where.bylines = { not: null }` (has partnership)
     - If `filters.hasBylines === false`: `where.bylines = null` (no partnership)

  In `types.ts`:
  1. Export a `SortField` type: `export type SortField = 'reachEstimate' | 'spendLower' | 'adDurationDays' | 'startDate' | 'createdAt';`
  2. Export a `SORT_OPTIONS` constant array for the frontend to consume:
     ```
     export const SORT_OPTIONS: { value: SortField; label: string }[] = [
       { value: 'reachEstimate', label: 'Reach' },
       { value: 'spendLower', label: 'Spend' },
       { value: 'adDurationDays', label: 'Days Active' },
       { value: 'startDate', label: 'Start Date' },
       { value: 'createdAt', label: 'Date Added' },
     ];
     ```
  3. Export a `GridDensity` type: `export type GridDensity = 'standard' | 'compact';` — needed by Plan 02/03 for grid density feature.
  </action>
  <verify>
  Run: `curl -s "http://localhost:3000/api/ad-library/ads?hasBylines=true&limit=3" | jq '.ads[:3] | .[].bylines'` — should return only ads with non-null bylines.
  Run: `curl -s "http://localhost:3000/api/ad-library/ads?hasBylines=false&limit=3" | jq '.ads[:3] | .[].bylines'` — should return only ads with null bylines.
  Run: `npx tsc --noEmit --pretty 2>&1 | head -20` — no type errors.
  </verify>
  <done>API filters by bylines presence/absence. SortField, SORT_OPTIONS, and GridDensity types are exported from types.ts for frontend use.</done>
</task>

</tasks>

<verification>
1. `curl "http://localhost:3000/api/ad-library/ads?sortBy=spendLower&sortOrder=desc&limit=5"` returns ads sorted by spend
2. `curl "http://localhost:3000/api/ad-library/ads?sortBy=adDurationDays&sortOrder=desc&limit=5"` returns ads sorted by days active
3. `curl "http://localhost:3000/api/ad-library/ads?hasBylines=true&limit=5"` returns only partnership ads
4. `curl "http://localhost:3000/api/ad-library/ads?hasBylines=false&limit=5"` returns only non-partnership ads
5. TypeScript compiles without errors
</verification>

<success_criteria>
- 5 sort fields work: reachEstimate, spendLower, adDurationDays, startDate, createdAt
- hasBylines=true returns only ads where bylines is not null
- hasBylines=false returns only ads where bylines is null
- No type errors, no regressions in existing sort/filter behavior
</success_criteria>

<output>
After completion, create `.planning/phases/47-filter-sort-overhaul/47-01-SUMMARY.md`
</output>
