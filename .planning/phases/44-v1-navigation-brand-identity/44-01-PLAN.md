---
phase: 44-v1-navigation-brand-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/analyser/page.tsx
autonomous: true

must_haves:
  truths:
    - "V1 analyser page displays BarChart3 icon + 'Ad Library Pro' brand lockup in header"
    - "V1 header has logo linking to / and upgrade CTA pill linking to /#pricing"
    - "Old navigation links (How it works, About, Contact, Feedback, Roadmap) are fully removed"
    - "No /coming-soon links remain anywhere on the page"
    - "Contextual upgrade card appears below analysis results when apiResult exists"
  artifacts:
    - path: "src/app/analyser/page.tsx"
      provides: "V1 analyser page with branded header and upgrade card"
      contains: "BarChart3"
  key_links:
    - from: "src/app/analyser/page.tsx"
      to: "landing-nav.tsx brand lockup pattern"
      via: "BarChart3 icon + 'Ad Library Pro' text in header"
      pattern: "BarChart3.*Ad Library Pro"
    - from: "src/app/analyser/page.tsx header CTA"
      to: "/#pricing"
      via: "Link component"
      pattern: "href.*/#pricing.*Get Pro"
    - from: "src/app/analyser/page.tsx upgrade card"
      to: "/#pricing"
      via: "Link component"
      pattern: "apiResult.*Explore Pro Dashboard"
---

<objective>
Replace the V1 analyser page's old navigation bar with a branded header matching the landing page identity, remove dead nav links, update the CTA, and add a contextual upgrade card below analysis results.

Purpose: Bring V1 branding in line with landing page and V2 dashboard so the product feels cohesive.
Output: Updated `src/app/analyser/page.tsx` with branded header and upgrade card.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-v1-navigation-brand-identity/44-RESEARCH.md
@src/app/analyser/page.tsx
@src/components/landing/landing-nav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace V1 nav with branded header and clean up dead code</name>
  <files>src/app/analyser/page.tsx</files>
  <action>
Replace the entire `<nav>` block (lines 435-524) in the analyser page with a minimal branded header matching the landing page pattern.

The new header structure:
```tsx
<nav className="border-b border-[var(--border-subtle)] bg-[var(--bg-primary)]/80 backdrop-blur-sm sticky top-0 z-50">
  <div className="max-w-7xl mx-auto px-6 h-14 flex items-center justify-between">
    <Link href="/" className="flex items-center gap-2.5 group">
      <div className="bg-[#1235e2] p-1.5 rounded-lg text-white transition-transform duration-300 ease-[cubic-bezier(0.32,0.72,0,1)] group-hover:scale-105">
        <BarChart3 className="w-4 h-4" strokeWidth={2} />
      </div>
      <span className="text-[15px] font-semibold text-[var(--text-primary)] tracking-tight">
        Ad Library Pro
      </span>
    </Link>
    <Link
      href="/#pricing"
      className="inline-flex items-center gap-2 bg-[#1235e2] hover:bg-[#0f2bc0] text-white text-sm font-medium px-5 py-2.5 rounded-full transition-all duration-300 ease-[cubic-bezier(0.32,0.72,0,1)] active:scale-[0.98]"
    >
      Get Pro
    </Link>
  </div>
</nav>
```

Key details:
- Use `var(--text-primary)` for the brand text (respects V1 theme), but hardcoded `#1235e2` for brand accent (same in both systems)
- Use Next.js `Link` component (not `<a>` tags) for both the logo link (`/`) and CTA (`/#pricing`)
- Add `BarChart3` to the lucide-react imports if not already present
- Add `import Link from 'next/link'` if not already present

After replacing the nav, clean up dead code:
1. Remove the `mobileMenuOpen` state variable and its setter (no longer needed -- the mobile hamburger menu is gone)
2. Remove `Menu` from lucide-react imports (only used for hamburger). Keep `X` -- it's used in `ActiveChartFilter`.
3. KEEP the `SubmitModal` component, its imports, and its state (`roadmapModalOpen`, `roadmapInitialQuery`) -- it has a non-nav trigger at line ~561 (`setRoadmapModalOpen(true)` in the "not found" search state)
4. KEEP `KiriMediaPopup` and `FeedbackPopup` -- they are unrelated to navigation

Verify no `/coming-soon` links remain anywhere in the file after the change.
  </action>
  <verify>
Run `npx next build` -- no build errors.
Grep the file for `/coming-soon` -- zero matches.
Grep the file for `mobileMenuOpen` -- zero matches.
Grep the file for `How it works` or `About` or `Contact` nav links -- zero matches.
Grep the file for `BarChart3` -- at least one match in the header.
Grep the file for `/#pricing` -- at least one match (CTA).
  </verify>
  <done>
V1 displays BarChart3 + "Ad Library Pro" brand lockup, logo links to /, CTA pill links to /#pricing, old nav links fully removed, no /coming-soon references remain, no dead code from removed mobile menu.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add contextual upgrade card below analysis results</name>
  <files>src/app/analyser/page.tsx</files>
  <action>
Insert a contextual upgrade card after the results section closes (after line ~1597-1599 where the `apiResult` conditional and results wrapper close) and before the footer comment (line ~1601).

The upgrade card should only render when the user has analysis results. Insert it INSIDE the existing `apiResult` conditional block, after the results content but before the closing of that block. This way it naturally only shows when results exist.

```tsx
{/* Upgrade Card */}
<div className="max-w-7xl mx-auto px-6">
  <div className="mt-12 mb-8 p-6 rounded-xl bg-[var(--bg-tertiary)] border border-[var(--border-subtle)] text-center">
    <BarChart3 className="w-8 h-8 text-[#1235e2] mx-auto mb-3" />
    <h3 className="text-lg font-bold text-[var(--text-primary)] mb-2">
      Want deeper insights?
    </h3>
    <p className="text-sm text-[var(--text-secondary)] mb-4 max-w-md mx-auto">
      Explore the full Ad Library Pro dashboard with saved brands,
      category browsing, and AI-powered creative analysis.
    </p>
    <Link
      href="/#pricing"
      className="inline-flex items-center gap-2 bg-[#1235e2] hover:bg-[#0f2bc0] text-white text-sm font-medium px-6 py-2.5 rounded-full transition-all duration-300 ease-[cubic-bezier(0.32,0.72,0,1)] active:scale-[0.98]"
    >
      Explore Pro Dashboard
    </Link>
  </div>
</div>
```

Key details:
- Uses `var(--bg-tertiary)`, `var(--border-subtle)`, `var(--text-primary)`, `var(--text-secondary)` -- V1's CSS custom properties
- Uses hardcoded `#1235e2` only for brand accent (consistent with brand lockup)
- CTA links to `/#pricing` using Next.js `Link` (same as header CTA)
- Placed inside the `apiResult` conditional so it only shows after analysis
- Wrapped in `max-w-7xl mx-auto px-6` to match the page's content width
  </action>
  <verify>
Run `npx next build` -- no build errors.
Grep the file for `Explore Pro Dashboard` -- one match.
Grep the file for `Want deeper insights` -- one match.
Grep the file for `/#pricing` -- at least two matches (header CTA + upgrade card CTA).
  </verify>
  <done>
Contextual upgrade card renders below analysis results when apiResult exists, with BarChart3 icon, persuasive copy, and CTA pill linking to /#pricing.
  </done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. No `/coming-soon` references in `src/app/analyser/page.tsx`
3. No `mobileMenuOpen` dead state in the file
4. `BarChart3` icon present in header brand lockup
5. "Ad Library Pro" text present in header
6. Logo links to `/`, both CTAs link to `/#pricing`
7. No old nav links (How it works, About, Contact, Feedback, Roadmap) in the file's nav section
8. Upgrade card present inside `apiResult` conditional
9. `SubmitModal` still present and functional (not accidentally removed)
</verification>

<success_criteria>
- V1 analyser page has branded header matching landing page identity (BarChart3 + "Ad Library Pro")
- Header contains logo link to / and "Get Pro" CTA pill to /#pricing
- All old navigation links removed, no /coming-soon references
- Upgrade card appears below results prompting users toward Pro dashboard
- Build passes with zero errors
- All 5 requirements (NAV-01 through NAV-05) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/44-v1-navigation-brand-identity/44-01-SUMMARY.md`
</output>
