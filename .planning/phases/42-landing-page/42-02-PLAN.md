---
phase: 42-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - src/app/page.tsx
  - src/components/landing/hero-section.tsx
  - src/components/landing/feature-showcase.tsx
  - src/components/landing/v2-preview-section.tsx
  - src/components/landing/pricing-section.tsx
  - src/components/landing/pro-cta.tsx
  - src/components/landing/landing-nav.tsx
  - src/components/landing/landing-footer.tsx
autonomous: false

must_haves:
  truths:
    - "Landing page at / presents a clear value proposition for Ad Library Pro"
    - "Landing page has a prominent CTA that links to /analyser for free trial"
    - "Landing page showcases V2 dashboard features with visual preview"
    - "Landing page displays $99/month Pro pricing with upgrade CTA"
    - "Unauthenticated visitors see CTAs (not hidden buttons)"
    - "Page loads fast as a Server Component (no 'use client' on page.tsx)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page Server Component composing all sections"
      min_lines: 20
    - path: "src/components/landing/hero-section.tsx"
      provides: "Hero with value proposition and primary CTA"
      min_lines: 30
    - path: "src/components/landing/feature-showcase.tsx"
      provides: "V2 feature highlights grid"
      min_lines: 40
    - path: "src/components/landing/v2-preview-section.tsx"
      provides: "Visual preview/glimpse of V2 dashboard"
      min_lines: 20
    - path: "src/components/landing/pricing-section.tsx"
      provides: "Free vs Pro comparison with $99/mo pricing"
      min_lines: 40
    - path: "src/components/landing/pro-cta.tsx"
      provides: "Client component CTA handling auth state"
      contains: "use client"
  key_links:
    - from: "src/components/landing/hero-section.tsx"
      to: "/analyser"
      via: "Try Free CTA link"
      pattern: "href.*analyser"
    - from: "src/components/landing/pro-cta.tsx"
      to: "createCheckoutSession"
      via: "Stripe checkout for authenticated users"
      pattern: "createCheckoutSession"
    - from: "src/components/landing/pro-cta.tsx"
      to: "/auth/signin"
      via: "Sign-in redirect for unauthenticated users"
      pattern: "signin.*callbackUrl"
    - from: "src/components/landing/pricing-section.tsx"
      to: "pro-cta.tsx"
      via: "ProCTA component import"
      pattern: "ProCTA"
---

<objective>
Build the landing page at `/` with hero, feature showcase, V2 preview, pricing, and conversion CTAs.

Purpose: This is the primary conversion page that drives visitors from free (V1 analyser) to Pro (V2 dashboard at $99/month). It must work for unauthenticated visitors as the primary audience.
Output: Complete landing page with all sections, using V2 blue design system.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-landing-page/42-RESEARCH.md
@.planning/phases/42-landing-page/42-01-SUMMARY.md

@src/app/coming-soon/page.tsx
@src/app/globals.css
@src/components/subscription/upgrade-button.tsx
@src/app/actions/stripe.ts
@src/app/dashboard/v2/v2-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page components</name>
  <files>
    src/components/landing/landing-nav.tsx
    src/components/landing/hero-section.tsx
    src/components/landing/feature-showcase.tsx
    src/components/landing/v2-preview-section.tsx
    src/components/landing/pricing-section.tsx
    src/components/landing/pro-cta.tsx
    src/components/landing/landing-footer.tsx
  </files>
  <action>
    Create `src/components/landing/` directory and all landing page section components. Use the V2 blue design system: `#1235e2` primary, `#101322` dark backgrounds, `#f6f6f8` light backgrounds. Use DM Sans for body text and Instrument Serif for headings (font-serif class). Reuse existing CSS utility classes: `.glass`, `.gradient-mesh`, `.noise-overlay`, `.animate-fade-in-up`, `.stagger-1` through `.stagger-5`.

    **landing-nav.tsx** (Server Component):
    - Sticky top nav with backdrop blur
    - Left: "Ad Library Pro" brand name with a small blue square icon (matching V2 shell style)
    - Right: "Try Free" link to `/analyser` and "Get Pro" button linking to `#pricing` section
    - Use V2 dark bg `#101322` for nav background

    **hero-section.tsx** (Server Component):
    - Large heading: "See what your competitors are running" (or similar value-prop headline using Instrument Serif italic)
    - Subheading: 1-2 lines explaining the tool helps brands/agencies browse, save, and analyze competitor Facebook ads
    - Primary CTA: "Try Free Analyser" linking to `/analyser` (blue button, `bg-[#1235e2]`)
    - Secondary CTA: "Get Pro - $99/mo" linking to `#pricing` anchor
    - Use `.animate-fade-in-up` for entrance animation
    - Dark background section (`bg-[#101322]` with white/light text)

    **feature-showcase.tsx** (Server Component):
    - Section heading: "Everything you need for competitive ad intelligence"
    - 6 feature cards in a 2-col (md) or 3-col (lg) grid using Lucide icons:
      1. Analytics Dashboard - BarChart3 icon - "KPI cards, timeline charts, format and platform breakdowns"
      2. Creative Lab - Sparkles icon - "Brand pillar analysis, creative diversity scores, hook analysis"
      3. Ad Library - Library icon - "Browse 50,000+ ads with filters, save favorites"
      4. Brand & Category Browsing - FolderOpen icon - "Organized by industry, easy discovery"
      5. Hikaru AI - Bot icon - "AI-powered analysis with interactive charts"
      6. Coming Soon: Downloads - Download icon - "Export reports and data (coming soon)" with subtle "coming soon" badge
    - Cards: dark card bg (`bg-[#101322]/50` or `bg-white/5`), rounded corners, subtle border, blue accent icon color
    - Light section background or subtle gradient

    **v2-preview-section.tsx** (Server Component):
    - Section heading: "A glimpse of Ad Library Pro"
    - Since we do NOT have actual screenshots yet, create a stylized mockup/wireframe representation:
      - A dark rounded container (`bg-[#101322]`) styled to look like a dashboard window
      - Inside: a simplified visual mockup showing a sidebar with nav items (Dashboard, Creative Lab, Ad Library, etc.) and a main content area with placeholder chart shapes and KPI card outlines
      - Use CSS only (divs with backgrounds, borders, rounded corners) to suggest the V2 dashboard layout
      - Add a subtle blue glow/border effect and "Ad Library Pro" label
    - Below the mockup: "Unlock the full dashboard" text with a CTA

    **pricing-section.tsx** (Server Component with client island):
    - Section with `id="pricing"` for anchor scroll
    - Two-column pricing comparison:
      - FREE tier: "Ad Analyser" - List what's included (brand search, demographics, ad previews, basic analysis). CTA: "Try Free" linking to `/analyser`
      - PRO tier: "$99/month" - "Ad Library Pro" - List everything in Free plus: Analytics Dashboard, Creative Lab, Full Ad Library with 50k+ ads, Saved Ads, Brand Tracking, Hikaru AI, Category Browsing, Downloads (coming soon). CTA: `<ProCTA />` component
    - PRO card should be visually emphasized (blue border, "Popular" badge, slightly larger)
    - Hardcode "$99/mo" text (Stripe enforces actual price at checkout)

    **pro-cta.tsx** (Client Component - 'use client'):
    - Handles auth-aware upgrade flow:
      - If user is authenticated: calls `createCheckoutSession()` server action directly (imports from `@/app/actions/stripe`)
      - If user is NOT authenticated: redirects to `/auth/signin?callbackUrl=/dashboard/v2` so they sign in first, then land in V2
    - Uses `useSession` from next-auth and `useRouter` from next/navigation
    - Button styled with blue gradient: `bg-[#1235e2] hover:bg-[#0f2bc0]` white text
    - Shows loading state during transition
    - Accepts optional `className` and `label` props for reuse in hero and pricing sections

    **landing-footer.tsx** (Server Component):
    - Simple footer: "Built for competitive research. Data sourced from Facebook Ad Library API."
    - Links: "Free Analyser" -> `/analyser`, "Dashboard" -> `/dashboard/v2`
    - Copyright line
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify all components have no type errors.
    Verify all files exist in `src/components/landing/`.
  </verify>
  <done>All 7 landing page section components created with V2 blue design system, proper Lucide icons, and auth-aware ProCTA.</done>
</task>

<task type="auto">
  <name>Task 2: Compose landing page at / route</name>
  <files>src/app/page.tsx</files>
  <action>
    Create `src/app/page.tsx` as a Server Component (NO 'use client' directive). This is the main landing page that composes all section components.

    Structure:
    ```
    import { LandingNav } from '@/components/landing/landing-nav';
    import { HeroSection } from '@/components/landing/hero-section';
    import { FeatureShowcase } from '@/components/landing/feature-showcase';
    import { V2PreviewSection } from '@/components/landing/v2-preview-section';
    import { PricingSection } from '@/components/landing/pricing-section';
    import { LandingFooter } from '@/components/landing/landing-footer';

    export const metadata = {
      title: 'Ad Library Pro — Competitive Ad Intelligence',
      description: 'Browse, save, and analyze competitor Facebook ads. Free analyser tool plus Pro dashboard with AI-powered insights.',
    };

    export default function LandingPage() {
      return (
        <main className="min-h-screen bg-[#f6f6f8]">
          <LandingNav />
          <HeroSection />
          <FeatureShowcase />
          <V2PreviewSection />
          <PricingSection />
          <LandingFooter />
        </main>
      );
    }
    ```

    The page exports `metadata` for SEO (conversion-focused title and description). The page itself is a thin composition layer. Each section handles its own styling and layout.

    Ensure smooth visual flow between sections — alternate between dark (`#101322`) and light (`#f6f6f8`) section backgrounds for visual rhythm. Hero = dark, Features = light, Preview = dark, Pricing = light, Footer = dark.
  </action>
  <verify>
    Run `npm run build` to verify the full build passes.
    Run `npm run dev` and visit `http://localhost:3000/` — the landing page should render.
    Visit `http://localhost:3000/analyser` — the V1 tool should still work.
  </verify>
  <done>Landing page renders at / as a Server Component. All sections visible. V1 tool still works at /analyser. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete landing page at / with hero, feature showcase, V2 preview mockup, pricing comparison (Free vs Pro at $99/mo), and auth-aware upgrade CTA. V1 tool moved to /analyser.</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/ — landing page should show with blue V2 design system
    2. Check hero section has clear value proposition and two CTAs (Try Free + Get Pro)
    3. Scroll to feature showcase — 6 features in a grid with icons
    4. Check V2 preview section — stylized mockup of the dashboard
    5. Check pricing section — Free vs Pro ($99/mo) comparison cards
    6. Click "Try Free Analyser" — should navigate to /analyser with the V1 tool working
    7. Click "Get Pro" — should either start Stripe checkout (if logged in) or redirect to sign-in
    8. Check mobile responsiveness — sections should stack properly on narrow viewport
    9. Verify page loads fast (no heavy JS bundle — check Network tab for page size)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. Landing page at `/` renders all sections (hero, features, preview, pricing, footer)
3. V1 tool at `/analyser` works identically to before
4. ProCTA handles both auth states (authenticated -> checkout, unauthenticated -> sign-in)
5. Page is a Server Component (check: no 'use client' in page.tsx, fast load)
6. SEO metadata is conversion-focused
7. Mobile responsive (grid collapses, text readable)
</verification>

<success_criteria>
- Landing page at / presents Ad Library Pro value proposition with V2 blue design
- "Try Free" CTA links to /analyser (V1 tool)
- "Get Pro - $99/mo" CTA works for both auth states
- V2 feature showcase with 6 features displayed
- V2 dashboard preview/glimpse section visible
- Free vs Pro pricing comparison with $99/month clearly displayed
- Page loads as Server Component (fast, SEO-friendly)
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/42-landing-page/42-02-SUMMARY.md`
</output>
