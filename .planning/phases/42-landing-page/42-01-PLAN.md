---
phase: 42-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/analyser/page.tsx
  - src/app/actions/stripe.ts
autonomous: true

must_haves:
  truths:
    - "V1 Ad Analyser tool is accessible at /analyser and works identically to the old /"
    - "Stripe checkout success redirects to /dashboard/v2 after payment, not back to landing page"
    - "The / route is free for a new landing page (no V1 tool there)"
  artifacts:
    - path: "src/app/analyser/page.tsx"
      provides: "V1 Ad Analyser tool at /analyser route"
      contains: "use client"
    - path: "src/app/actions/stripe.ts"
      provides: "Updated checkout URLs pointing to /dashboard/v2"
      contains: "/dashboard/v2"
  key_links:
    - from: "src/app/actions/stripe.ts"
      to: "/dashboard/v2"
      via: "success_url redirect"
      pattern: "dashboard/v2.*upgrade=success"
---

<objective>
Move the V1 Ad Analyser from `/` to `/analyser` and fix Stripe checkout redirect URLs.

Purpose: Free up the `/` route for the new landing page, and ensure post-payment redirect goes to the V2 dashboard instead of back to the marketing page.
Output: V1 tool working at `/analyser`, Stripe URLs pointing to `/dashboard/v2`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-landing-page/42-RESEARCH.md

@src/app/page.tsx
@src/app/actions/stripe.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move V1 Ad Analyser to /analyser route</name>
  <files>src/app/analyser/page.tsx, src/app/page.tsx</files>
  <action>
    1. Create `src/app/analyser/` directory.
    2. Move `src/app/page.tsx` to `src/app/analyser/page.tsx`. The file content stays exactly the same -- it is a 'use client' component called `Home()` with all its imports. No changes needed to the component itself since all imports use `@/` aliases.
    3. Delete the original `src/app/page.tsx` (it will be recreated in Plan 02 as the landing page).
    4. In the V1 page's nav section, update any self-referencing links. The nav currently has links like `href="/"` for logo and navigation items. Update the logo link to point to `/` (landing page) and any "back" type links. The "Pro -- Coming Soon" nav CTA link that points to `/coming-soon` should be updated to point to `/dashboard/v2` or kept as-is for now.
    5. In `src/app/coming-soon/page.tsx`, update the "Back to Analyser" link from `href="/"` to `href="/analyser"`, and the "Try the free analyser" link from `href="/"` to `href="/analyser"`. Also update the logo link.
  </action>
  <verify>
    Run `npm run build` -- the build should succeed with no errors about missing pages.
    Verify `src/app/analyser/page.tsx` exists and `src/app/page.tsx` does NOT exist.
  </verify>
  <done>V1 tool lives at /analyser route. The / route is empty (no page.tsx). All internal links updated.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Stripe checkout redirect URLs</name>
  <files>src/app/actions/stripe.ts</files>
  <action>
    In `src/app/actions/stripe.ts`, update the `createCheckoutSession` function:

    1. Change `success_url` from:
       `${process.env.NEXT_PUBLIC_APP_URL}/?upgrade=success`
       to:
       `${process.env.NEXT_PUBLIC_APP_URL}/dashboard/v2?upgrade=success`

    2. Change `cancel_url` from:
       `${process.env.NEXT_PUBLIC_APP_URL}/?upgrade=cancelled`
       to:
       `${process.env.NEXT_PUBLIC_APP_URL}/dashboard/v2?upgrade=cancelled`

    3. In the `createPortalSession` function, update `return_url` from:
       `${process.env.NEXT_PUBLIC_APP_URL}/`
       to:
       `${process.env.NEXT_PUBLIC_APP_URL}/dashboard/v2`

    These are simple string replacements. Do not change any other logic.
  </action>
  <verify>
    Read `src/app/actions/stripe.ts` and confirm all three URLs now reference `/dashboard/v2`.
    Run `npx tsc --noEmit` to verify no type errors.
  </verify>
  <done>Stripe checkout success/cancel/portal return URLs all redirect to /dashboard/v2 instead of /.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `src/app/analyser/page.tsx` exists with the full V1 component
3. `src/app/page.tsx` does NOT exist (ready for Plan 02)
4. `src/app/actions/stripe.ts` contains `/dashboard/v2` in all redirect URLs
5. No broken imports (all use `@/` aliases so the move is safe)
</verification>

<success_criteria>
- V1 Ad Analyser accessible at /analyser with all functionality intact
- / route is clear for the new landing page
- Stripe redirects point to /dashboard/v2
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/42-landing-page/42-01-SUMMARY.md`
</output>
